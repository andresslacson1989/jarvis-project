# Future Linux composition namespace — no V1 runtime

This source namespace is intentionally reserved for a future Linux `FULL_HOST`
backend. Windows V1 does not register, instantiate, package, or claim a Linux
runtime backend here. A future synchronous contract/release-profile change must
select and qualify the Linux-native mechanisms before executable backend code is
introduced.
