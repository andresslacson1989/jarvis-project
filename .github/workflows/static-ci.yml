name: Static CI

on:
  push:
    branches:
      - master
      - 'impl/**'
  pull_request:

permissions:
  contents: read

concurrency:
  group: static-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  windows-desktop:
    name: windows-desktop
    runs-on: windows-2025
    timeout-minutes: 20
    steps:
      - name: Checkout Windows candidate
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
          fetch-depth: 1

      - name: Install pinned Windows pnpm and Node
        uses: pnpm/setup@84cb39b217b10273981911c288cd62326dc7c6d2
        with:
          version: 11.21.0
          runtime: node@24.18.0
          install: false
          cache: false

      - name: Install pinned Windows Rust toolchain
        shell: pwsh
        run: |
          rustup set profile minimal
          rustup toolchain install 1.97.1 --target x86_64-pc-windows-msvc
          rustup default 1.97.1

      - name: Install Windows dependencies without lifecycle scripts
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Verify exact Windows toolchain
        run: pnpm toolchain:verify

      - name: Build bundled local desktop frontend on Windows
        run: pnpm --filter @jarvis/desktop build:web

      - name: Native Windows MSVC Tauri compile
        run: cargo check --locked --workspace --target x86_64-pc-windows-msvc

  static-ci:
    name: static-ci
    needs: windows-desktop
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
          fetch-depth: 1

      - name: Install pinned pnpm and Node
        uses: pnpm/setup@84cb39b217b10273981911c288cd62326dc7c6d2
        with:
          version: 11.21.0
          runtime: node@24.18.0
          install: false
          cache: false

      - name: Install pinned Rust toolchain
        shell: bash
        run: |
          rustup set profile minimal
          rustup toolchain install 1.97.1 --component rustfmt --component clippy
          rustup default 1.97.1

      - name: Install dependencies without lifecycle scripts
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Verify exact toolchain
        run: pnpm toolchain:verify

      - name: Format hygiene
        run: pnpm format:check

      - name: Schema integrity
        run: pnpm schema:check

      - name: Generated contract artifacts
        run: pnpm contract:check-generated

      - name: Contract manifest
        run: pnpm contract:check-manifest

      - name: Contract and profile drift
        run: pnpm contract:check-drift

      - name: Repository governance
        run: pnpm governance:check

      - name: Secret scan
        run: pnpm security:secrets

      - name: Dependency inventory
        run: pnpm dependency:check

      - name: License and provenance
        run: pnpm provenance:check

      - name: TypeScript strict typecheck
        run: pnpm typecheck

      - name: TypeScript build
        run: pnpm build

      - name: Architecture enforcement
        run: pnpm architecture:check

      - name: Normal deterministic tests
        run: pnpm test

      - name: Node dependency vulnerability gate
        run: pnpm audit --audit-level high

      - name: Install pinned RustSec audit tool
        run: cargo install cargo-audit --version 0.22.2 --locked

      - name: Rust dependency vulnerability gate
        run: cargo audit

      - name: Rust formatting
        run: cargo fmt --all -- --check

      - name: Rust clippy warnings-as-errors
        run: cargo clippy --locked -p jarvis-toolchain-smoke --all-targets --all-features -- -D warnings

      - name: Rust host build
        run: cargo check --locked -p jarvis-toolchain-smoke --all-targets --all-features

      - name: Phase 0 section checkpoint
        run: pnpm phase0:check

      - name: Static CI evidence
        env:
          JARVIS_STATIC_CI_GATES_PASSED: '1'
          JARVIS_PHASE0_CHECKPOINT_PASSED: '1'
        run: pnpm ci:evidence
