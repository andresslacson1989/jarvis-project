// Node 22 test-loader bridge. The production Core build emits the typed
// release-trust.ts module as release-trust.js, while source tests execute the
// TypeScript tree directly.
export * from "./release-trust.ts";
