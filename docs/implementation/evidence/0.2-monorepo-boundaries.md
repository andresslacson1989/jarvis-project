# JARVIS 0.2 Monorepo Boundary Evidence

**Subsection:** `0.2 — Monorepo responsibility boundaries and dependency-direction skeleton`  
**Contract suite:** JARVIS v1.0.5  
**Implementation branch:** `impl/phase0-0.1-admission`

> Non-normative implementation evidence. The active contract suite remains the sole implementation authority.

## Goal

Materialize the repository responsibility namespaces and explicit dependency-direction boundary needed before platform/protocol/toolchain implementation, without implementing later Phase-0 responsibilities.

## Governing references

- `docs/implementation/JARVIS-IMPLEMENTATION-PLAN.md` §2.
- `docs/implementation/JARVIS-CODING-STANDARDS-CONTRACT.md` §§2–4 and §32.
- `docs/JARVIS-IMPLEMENTATION-CONTRACT-v1.0.5.md` §§5 and 8.

## Preconditions

- `0.1` is `VERIFIED` in the live implementation branch matrix.
- Live `master` was re-fetched before this repository write and remained `bb32c5308afb4ee757493b5822dffef49dff151e`.
- The governing contract text was reread from that live authority.

## Required outputs implemented by this change

- Tracked desktop presentation/native-shell boundaries.
- Tracked authoritative Core boundary.
- Tracked protocol/schema/policy/platform-contract/shared package boundaries.
- Tracked Windows native backend namespace.
- Tracked future-Linux namespace with an explicit no-V1-runtime/no-support rule.
- Tracked AI/speech/integration adapter boundaries.
- Tracked platform/Git/filesystem/project tool boundaries.
- Tracked managed-module boundary.
- Reserved test responsibility namespace without pre-implementing subsection 0.9.
- Central repository responsibility/dependency-direction map.

## Hard acceptance criteria

1. Contract Accuracy is exactly `10/10`; no normative contract is changed.
2. Every Coding Standards §2 responsibility has a tracked repository owner or explicit reserved responsibility boundary.
3. UI, Core, native host, platform backends, providers, tools, modules, and shared packages do not acquire authority outside their contract roles.
4. Shared/domain/protocol/policy boundaries explicitly reject native-backend imports/type leakage.
5. `packages/policy` is explicitly deterministic/side-effect-free and `packages/shared` explicitly narrow/non-domain.
6. Linux is visibly future-only and receives no runtime implementation/support claim.
7. No toolchain/package/workspace manifest, runtime implementation, schema implementation, test harness, CI workflow, or native backend code is introduced early.
8. No directory or adapter presence is represented as `SUPPORTED` capability evidence.

## Verification / adversarial checks

The verification pass must inspect the committed Git tree and diff and prove:

- all intended boundary files are tracked;
- only subsection-0.2 structural/evidence files were added in the implementation pass;
- no protected contract-controlled file was changed by the implementation pass;
- no `package.json`, lockfile, `Cargo.toml`, CI workflow, executable source, or native backend implementation was introduced;
- Linux wording explicitly rejects a V1 runtime/support claim;
- the central map preserves the contract's authority and dependency direction.

Runtime/build/platform tests are not applicable yet because subsection 0.2 intentionally creates no runtime or toolchain. Architecture/import enforcement becomes executable in subsection 0.10.

## Atomicity applicability

Runtime/SQLite atomicity is not applicable. The structural boundary change is staged as one scoped Git tree/commit before branch publication; no failed Git write is treated as success.

## Idempotency / retry safety

Paths are stable and unique by responsibility. A retry must reconcile current `master` and branch identity and update the same boundary paths rather than create competing package layouts.

## Failure / recovery

If authoritative `master` moves before a repository write, the write is stopped, the concurrent change is inspected/reconciled, and valid work is preserved. A stale Git ref update is not force-applied.

## Security / least privilege

This subsection introduces no secret, credential, process, network, filesystem-execution, or native privilege mechanism. The structure is specifically designed to prevent later code from conflating presentation, domain authority, provider authority, and native platform authority.

## Observability / diagnostics

The central boundary map plus the implementation matrix provides durable ownership, scope, evidence, current gap, and continuation point for later architecture-enforcement work.

## Initial score before verification

- Contract Accuracy: `10/10`.
- Production Readiness: `8/10`.
- Production Practices: `9/10`.
- Enterprise Hardening: `9/10`.
- Atomicity: N/A.
- Idempotency / Retry Safety: `9/10`.
- Failure / Recovery: `9/10`.
- Security / Least Privilege: `9/10`.
- Observability / Diagnostics: `9/10`.
- Test / Verification Quality: `8/10` pending committed-tree/diff verification.
- Maintainability / Architecture Integrity: `9/10`.

**Weakest remaining gap before verification:** prove the committed tree contains exactly the intended responsibility skeleton and no later-subsection/toolchain/runtime/native implementation or protected-contract mutation.
