# 0.13 — Authoritative `master` Repository Governance Verification Evidence

## Status

**VERIFIED — JARVIS v1.0.7 `SERVER_ENFORCED` mode.**

This current governance record supersedes the earlier v1.0.6/v1.0.7
`COMPENSATING_CONTROLS` observation. The earlier fallback evidence remains
historical provenance below; it is not the current governance state.

## Goal

Qualify authoritative-repository governance from a fresh authenticated
hosting/account capability observation. When server-side protection is
available, `master` must use the effective server-enforced policy required by
the active contract.

## Governing references

- `docs/JARVIS-CONTRACT-MANIFEST-v1.0.7.md`
- `docs/JARVIS-IMPLEMENTATION-CONTRACT-v1.0.7.md` §28
- `docs/implementation/JARVIS-IMPLEMENTATION-PLAN.md` Phase 0 / Required repository governance
- `docs/implementation/JARVIS-VERIFICATION-RELEASE-CONTRACT.md` §33
- `docs/decisions/ADR-074-hosting-capability-aware-repository-governance.md`
- `docs/decisions/ADR-076-qualified-ci-authority-equivalence.md`
- `docs/implementation/governance/MASTER-PROTECTION.md`
- `docs/implementation/governance/repository-governance-profile.json`

## Fresh authenticated hosting-capability observation

Observation timestamp:

```text
2026-09-12T22:26:31Z
```

The authenticated GitHub API returned the following sanitized results:

```text
repository: andresslacson1989/jarvis-project
visibility: public
default branch: master
GET /repos/.../branches/master/protection before configuration: HTTP 404
PUT /repos/.../branches/master/protection: HTTP 200
GET /repos/.../branches/master/protection after configuration: HTTP 200
GET /repos/.../rulesets?includes_parents=true: HTTP 200, 0 rulesets
```

The post-configuration branch observation returned:

```text
branch: master
commit: bb59c13d99c8b472de0dbe08b8f5ce59cf50e705
protected: true
```

The HTTP 404 was the pre-configuration absence of a branch-protection resource,
not proof that the hosting capability was unavailable. The successful
authenticated update and subsequent protected-branch read establish that
server-side protection is available for the current public repository/account.

## Effective server-enforced policy

The post-configuration response proved:

```text
required status checks: static-ci
strict/up-to-date required checks: true
required status-check app: GitHub Actions (app_id 15368)
required pull-request approvals: 1
dismiss stale reviews: true
required conversation resolution: true
administrators covered: true
force pushes allowed: false
branch deletion allowed: false
configured restrictions/bypass actors: none
```

The effective response reported `enforce_admins.enabled=true`,
`allow_force_pushes.enabled=false`, `allow_deletions.enabled=false`, and
`restrictions=null`. This is server-enforced protection; it is not described
as a compensating process control.

## Selected CI authority

The selected mandatory CI authority remains `GITHUB_ACTIONS`. Its qualified
pipeline identity is `.github/workflows/static-ci.yml` / `static-ci`. The
active suite still recognizes `GITHUB_ACTIONS` and `LOCALCI` as equal
eligible authority types after their separate authority-specific
qualification requirements are met. LocalCI remains unselected and
unqualified.

The existing durable Phase 0 authority record remains separately bound to
candidate `052902bfc52e676910d287e13fbf8a026915efe0` and authoritative-master
verification `2cfcca9f2f7ee0faf334223b659e94ab410f5acd` / run
`33950976184`. The current live master observation above must not be
silently substituted into that historical qualification identity.

The latest observed successful Static CI run for the current live master was:

```text
run_id: 34198981471
head_sha: bb59c13d99c8b472de0dbe08b8f5ce59cf50e705
event: push
branch: master
conclusion: success
```

That run records current branch state and is not the Section 1.4 candidate
qualification or its post-integration proof.

## Section 1.4 documentation-revision validation

The documentation-only revision that corrected the Section 1.4 evidence record
was independently validated before this governance reconciliation:

```text
documentation SHA: 367a3bdf8b083084abe9002783ddf9a6a1579bf6
GitHub Actions run: 34688940911
windows-tauri-build job: 103540708316 — SUCCESS
static-ci job: 103541583217 — SUCCESS
Tauri artifact: 10296976203, sha256:626d75ae94e5de0911bdaf5145f9e4821d225e032307a9f4a30c2d92746ce9c1
native artifact: 10296786503, sha256:92800513af3496f8baadf6d302ee04ec4c05d105355148d333c309edfa5d28ae
```

The retained downloaded evidence and repository verifier passed for both
qualification scopes. This validation remains separate from implementation
candidate `97a6fc19388f94cd82c65914fd48859b57557e75` and its exact candidate
run `34686091847`. It does not replace the implementation evidence or
authorize Section 1.4 integration.

## Historical fallback evidence — superseded

Before the current capability observation, the repository used the explicitly
verified v1.0.6/v1.0.7 fallback record:

```text
prior governance mode: COMPENSATING_CONTROLS
prior observation: authenticated administration returned HTTP 403
prior message: Upgrade to GitHub Pro or make this repository public to enable this feature.
prior corrected candidate: 8996d6947fd60b415e32485ee3777914f80cd590
prior pre-integration run: 31637736770
prior authoritative-master run: 31637928189
```

That fallback was contract-valid only while the required server-side capability
was unavailable. It is retained as historical audit evidence and is no longer
the selected mode. The current profile no longer records the old 403 as the
hosting capability observation or carries the fallback residual-risk claim.

## Atomicity, idempotency, and recovery

Repository protection configuration is an external administrative mutation,
not a shared JARVIS transaction. The configuration was applied through one
authenticated GitHub API update and verified by independent reads of the
resulting branch and protection state.

Implementation integration remains conditional on a fresh live-tip read,
exact-candidate CI, non-force ref movement, and post-integration verification.
An ambiguous administrative or ref-update result must be reconciled from live
GitHub state before another mutation. A changed branch policy requires a fresh
governance observation and evidence revision; prior fallback evidence cannot be
silently reused.

## Scores

| Criterion | Score | Basis |
|---|---:|---|
| Contract Accuracy | **10/10** | Current active §28/§33 server-enforced branch-governance semantics are recorded without retaining the old fallback as current. |
| Production Readiness | **9/10** | Effective server controls are live and independently read back; Section 1.4 lifecycle evidence remains separately pending. |
| Production Practices | **9/10** | Authenticated capability observation, explicit pre/post status, narrow policy, and post-write readback are recorded. |
| Enterprise Hardening | **9/10** | Force-push/deletion blocking, strict CI, admin enforcement, review, and conversation controls are active. |
| Atomicity | **9/10** | The external configuration mutation is bounded and read back; no false cross-system transaction claim. |
| Idempotency / Retry Safety | **9/10** | Reads are repeatable and future changes require fresh observation rather than blind retry or reuse. |
| Failure / Recovery | **9/10** | The stale 403 was diagnosed as historical, the available capability was configured, and the effective state was reverified. |
| Security / Least Privilege | **9/10** | The effective policy blocks force/deletion and limits administrative bypass; no broad bypass actors are configured. |
| Observability / Diagnostics | **10/10** | Sanitized API statuses, timestamps, branch SHA, effective rules, and separate CI identities are recorded. |
| Test / Verification Quality | **9/10** | Live API readback and existing governance negative/server-mode tests pass; final candidate CI remains a separate lifecycle gate. |
| Maintainability / Architecture Integrity | **9/10** | The machine-readable profile and operational evidence now reflect the active capability without changing normative contracts. |

## Gaps

No current hard gate remains inside subsection `0.13` after this live
governance requalification. Section 1.4 still requires its independent audit
decision, permitted integration, resulting authoritative-master verification,
and Section 1 checkpoint before its status can change.

## Call

**CALL: PASS — `0.13` remains VERIFIED under `SERVER_ENFORCED` governance.**

The current active implementation target remains Section 1.4. No Section 1.5
work is authorized or staged by this governance correction.
