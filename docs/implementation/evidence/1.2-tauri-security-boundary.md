# Section 1.2 — Tauri/WebView Security Boundary Handoff

## Original goal

Implement only the Tauri capabilities, CSP, navigation, external-link, devtools, and inert-content security boundary for the Windows desktop foundation. Do not begin Section 1.3 or claim Section 1.2 verified without the required qualification and independent review.

## Authority and candidate

- Authoritative base used: `origin/master` at `69d4378d...` when the branch was created.
- Candidate branch: `codex/section1-2-tauri-security`.
- Implementation candidate under audit: `052902bfc52e676910d287e13fbf8a026915efe0`.
- Documentation/evidence revisions `1f99f53c10ce0d406429f9888c763e27359260ee` and `a077dfdacd80f7ef5ef48d64f5cc61cec4fae70d` are not implementation candidates and intentionally contain no self-referential candidate binding.
- Earlier candidate `f9842cf` and intermediate remediation candidates are historical only and do not represent the current audit scope.
- Prior implementation/evidence commits: `5db16f3`, `d247325`, `b0f0920`, `941ff44`; evidence reconciliation commits: `b693fcc`, `5b3a669`, `445ac68`, `2372e06`, `44f6e8e`.
- Documentation-only evidence commits do not change the implementation candidate and are intentionally not self-referential.
- Draft review: PR #11.
- Section 1.2 status: `VERIFYING`; the implementation candidate has independent audit approval, but controlled integration and post-integration verification remain outstanding.

## Completed / found

- Added the minimal `main-local-ui` capability with only the bounded opener permission.
- Added production CSP with self-only executable/connectable origins and a separate loopback development CSP.
- Moved native window creation into the Rust composition root.
- Restricted production navigation to the exact bundled `http://tauri.localhost` origin with no explicit port; obsolete `tauri://localhost`, remote origins, and unexpected ports are rejected.
- Restricted debug navigation to the loopback development server.
- Denied Tauri new-window requests.
- Disabled production devtools in the native window configuration.
- Added a bounded external-link helper accepting only HTTP(S) URLs and delegating opening outside the privileged WebView.
- Preserved the existing inert bundled renderer; no HTML/Markdown execution path was introduced.
- Added positive and negative security-boundary tests.
- Hardened CSP validation to reject unexpected production/development origins.
- Bound debug navigation to loopback port `5173`.
- Added fail-closed inert-renderer execution-path checks.
- Replaced regex-only external-link acceptance with parsed HTTP(S) URL validation.
- Committed the empty-feature Cargo compatibility form and validated the clean candidate.
- Extended inert-content scanning across the complete renderer source tree and `index.html`.
- Rejected duplicate CSP directives instead of allowing map overwrite semantics.
- Corrected the existing Tauri pairing mutation regression so the test remains valid after the opener dependency was added.

## Changed files and purpose

- `apps/desktop/src-tauri/capabilities/main-local-ui.json` — minimal capability allowlist.
- `apps/desktop/src-tauri/Cargo.toml` — pinned opener plugin dependency.
- `apps/desktop/src-tauri/src/main.rs` — native navigation, window, devtools, and new-window policy.
- `apps/desktop/src-tauri/tauri.conf.json` — local production/development CSP and native-window configuration.
- `apps/desktop/src/external-link.ts` — bounded HTTP(S) external-link boundary.
- `apps/desktop/package.json` — pinned JavaScript opener plugin.
- `package.json` — desktop security checker command.
- `tools/ci/check-desktop-security.mjs` — fail-closed static security validator.
- `tests/layers/unit/desktop-security-boundary.test.mjs` — positive and negative security tests.
- `tests/layers/unit/desktop-foundation.test.mjs` — maintained Tauri pairing mutation regression.
- `docs/implementation/JARVIS-IMPLEMENTATION-MATRIX.md` — truthful active status/evidence update.

## Verification evidence

- `pnpm desktop:security:check` — PASS.
- Focused desktop security tests — 4/4 PASS.
- Full local normal profile — 21 test files PASS.
- Desktop TypeScript/Vite production build — PASS.
- Rust formatting check — PASS.
- Windows-target `cargo check --locked -p jarvis-desktop --target x86_64-pc-windows-msvc` — PASS.
- `git diff --check` — PASS.
- Prior candidate `6c72d9f...` normal profile — 21 files PASS; targeted LocalCI/evidence/governance suite — 49 tests PASS.
- LocalCI worker guard — Linux/WSL spoof rejected (exit 78); native MINGW Windows/X64 signature accepted.
- Additional CSP-origin, wrong-port, and external-link rejection tests — PASS.
- Duplicate-CSP and full-renderer inert-content checks — PASS.
- Exact-SHA GitHub CI run `33818720345` executed an earlier candidate and passed the native Windows/Tauri job, but failed closed on the Node dependency vulnerability gate.
- Candidate-bound qualification: GitHub Actions run `33934840029` passed exact checkout, Windows Tauri, complete static/security/architecture gates, and Phase 0 evidence for implementation candidate `052902bfc52e676910d287e13fbf8a026915efe0`; Windows job `101220622635` and static job `101222623099` passed.
- Documentation/evidence consistency revision `a077dfdacd80f7ef5ef48d64f5cc61cec4fae70d` passed GitHub Actions run `33938490684` (Windows job `101231030877`, static job `101232608613`) without changing implementation bytes. The earlier reconciliation revision `1f99f53c10ce0d406429f9888c763e27359260ee` was retained as negative evidence: run `33936789279`, static job `101228227719`, failed closed with `PHASE0_CANDIDATE_MISMATCH` because that documentation revision incorrectly added a machine-bound candidate field while CI supplied the current revision through `JARVIS_CANDIDATE_SHA`. The a077 revision removed that self-referential binding; explicit and implicit Phase 0 checks pass locally and the exact GitHub consistency run passed.

## Scope exclusions

Not implemented: Core IPC, provider execution, permissions, credentials, supervision, CSP/runtime configuration beyond this subsection, Mission Control features, Section 1.3 work, or canonical branding. The checked-in renderer remains inert bundled content.

## Remaining gates

1. Preserve the exact candidate qualification and reconciled evidence records through the controlled checkpoint.
2. Controlled non-force integration and post-integration authoritative verification.

## Key decisions

- Remote origins receive no privileged capability permissions.
- External links are bounded to HTTP(S) and opened through the external opener boundary.
- Missing or weakened security configuration fails the local checker.
- Local green results are recorded as local evidence only; they are not represented as GitHub Actions results.

## Next eligible target

Remain on Section 1.2 until exact-SHA CI, independent audit, controlled integration, and authoritative post-integration verification are resolved. Section 1.3 must not begin or be pre-staged.
