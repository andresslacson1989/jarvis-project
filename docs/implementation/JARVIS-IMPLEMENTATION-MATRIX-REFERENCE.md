# JARVIS Implementation Matrix — Reference Plan

This file is the stable planning/traceability half of the implementation matrix split.

**Important:** the canonical live execution pointer, current statuses, scores, gaps, and evidence are maintained in `docs/implementation/JARVIS-IMPLEMENTATION-MATRIX.md`. This reference file preserves the full pre-split matrix plan, dependency graph, subsection definitions, contract traceability, coverage indexes, release-gate mapping, and design-assurance material. Status-like fields embedded in the preserved pre-split snapshot below are historical as of the split and MUST NOT override the canonical live status file.

When resuming implementation, read `AGENTS.md`, then read the live matrix file first, then use this file for the detailed definition/dependencies/traceability of the active or next subsection.

---

