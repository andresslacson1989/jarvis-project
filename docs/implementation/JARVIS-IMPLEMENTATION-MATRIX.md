# JARVIS Implementation Execution Matrix — Live Control Board

**Document role:** Non-normative live execution/status control board for the JARVIS v1.0.7 private/internal implementation.

## Matrix split

The implementation matrix is intentionally split to keep frequent status/evidence writes small and auditable:

- `docs/implementation/JARVIS-IMPLEMENTATION-MATRIX.md` — **live control board**. This file owns the current execution pointer, current subsection status, scores, gaps, evidence, and the detailed rows for the currently active section.
- `docs/implementation/JARVIS-IMPLEMENTATION-MATRIX-REFERENCE.md` — **stable reference plan**. This file preserves the complete pre-split matrix dependency graph, subsection definitions, contract traceability, coverage indexes, release-gate mapping, and design assurance.

The two files together are one non-normative implementation matrix. They are not competing sources of truth. The active normative contract suite remains the implementation authority.

**Precedence rule:** for status, score, gap, evidence, and execution-pointer fields, this live file is authoritative. Status-like values in the reference snapshot are frozen historical values from the split baseline and MUST NOT override this file.

**Future-agent workflow:** read `AGENTS.md`, then this live file, then the reference file for the active/next subsection's full definition and dependencies. When a new parent section becomes active, promote that section's rows from the reference file into this live file verbatim before implementation, preserve the section/subsection hierarchy, and keep completed-section summaries auditable. Do not rewrite the large reference file for routine progress updates.

## Current execution pointer

| Field | Current value |
|---|---|
| Active section | `SECTION 7 — Codex Windows Provider Setup / Sandbox Qualification` |
| Active subsection | `7.5` — Qualified setup-helper identity validation and bounded UAC through PlatformPrivilegeMediator |
| Next eligible subsection | `7.5` — Qualified setup-helper identity validation and bounded UAC through PlatformPrivilegeMediator |
| Contract suite | JARVIS v1.0.7 |
| Authoritative implementation sequence | `docs/implementation/JARVIS-IMPLEMENTATION-PLAN.md` |
| Reference plan | `docs/implementation/JARVIS-IMPLEMENTATION-MATRIX-REFERENCE.md` |
| Production Complete | **NO** |

## Live implementation matrix

### SECTION 0 — Repository / Platform Contracts / Toolchain / Governance

| Section / Subsection | Status | Depends On | Governing Contract / Traceability | Score | Current Gap | Evidence / Result |
|---|---|---|---|---:|---|---|
| **SECTION 0 — Repository / Platform Contracts / Toolchain / Governance** | **VERIFIED** | — | `Phase 0`; checkpoint: Repository Governance + Platform Boundary + Contract-Drift Protection Ready | 10 CA / 8 scoped | None | `0.1–0.13` and `0.CP` verified; authoritative Phase 0 implementation integrated non-force to `master` at `34a05bd2da4c68583dcfe149efdbaa93a5bc0047`; post-integration run `31640209796` PASS |
| ↳ **0.1** Implementation admission, authoritative-suite pinning, and repository baseline | **VERIFIED** | — | PLAN §2; IC §§2–3,28–29; RP §17; AGENTS Source of truth/Branch authority | 10 CA / 9 scoped | None within `0.1` scope | `ded8f9e5c0e056ff69fa27668dfb0bd4fca7a0f7`; `docs/implementation/evidence/0.1-implementation-admission.md`; live `master` `bb32c530...`; diff verification changed only the evidence file |
| ↳ **0.2** Monorepo responsibility boundaries and dependency-direction skeleton | **VERIFIED** | 0.1 | PLAN §2; CS §§2–4,32; IC §§5,8 | 10 CA / 9 scoped | None within `0.2` scope | `67fa540caab982dccb2330070df62c6d71603099`; `docs/implementation/architecture/REPOSITORY-BOUNDARIES.md`; `docs/implementation/evidence/0.2-monorepo-boundaries.md`; diff added only 22 Markdown boundary/evidence files; no package/toolchain/runtime/native implementation; Linux negative-support wording verified |
| ↳ **0.3** Platform/runtime identity and compatibility protocol foundation | **VERIFIED** | 0.2 | PLAN §2; PP §§2–7,11; PS §§2–3,27; RP §§2–3 | 10 CA / 9 scoped | None within `0.3` scope | `229954d1e34c52bb11c50570308e9f66c2a21034`; `669fec6bad6cdec53a1a44fc94be3573e120997f`; `docs/implementation/evidence/0.3-platform-runtime-identity.md`; exact 6-file implementation; 5/5 schema self-validation; 20/20 positive/negative/adversarial cases; strict TypeScript auxiliary check PASS; no later-phase implementation leakage |
| ↳ **0.4** Semantic platform-capability contracts, availability, and stable platform errors | **VERIFIED** | 0.3 | PLAN §2; PP §§6,26–28; CS §4; RP §3 | 10 CA / 9 scoped | None within `0.4` scope | `b5bcd1b05128e65123b2bac95aa7b59d308d458a`; `a4dcf8593137779fc7a1ce4be63ff6d70acf3aef`; `docs/implementation/evidence/0.4-platform-capability-contracts.md`; exact 3-file implementation; strict TypeScript PASS; 8/8 behavioral/negative tests; 2/2 negative compile-time checks; no native-backend/toolchain/CI leakage |
| ↳ **0.5** Composition root, Windows backend registration, and future-Linux namespace reservation | **VERIFIED** | 0.4 | PLAN §2; PP §§7–10; CS §§2–4,32 | 10 CA / 9 scoped | None within `0.5` scope | `384be644b63f82da7397769066a595a9afa5a358`; `186dd001ff481c836a3e5074f59181e8b3152c8d`; `docs/implementation/evidence/0.5-platform-composition-root.md`; exact 4-file implementation; strict TypeScript PASS; 9/9 behavioral/negative checks; forbidden native/OS-branch scan PASS; Linux namespace contains no V1 executable runtime registration |
| ↳ **0.6** Protocol/schema infrastructure and common boundary primitives | **VERIFIED** | 0.3 | PLAN §2; PS §§1–9,25–27; CS §§5–7,11 | 10 CA / 9 scoped | None within `0.6` scope | `b1db7fda14e1d93753000108510833168f7d04b6`; `b84ad8fab42f8f84305b71dcae9160fce5a015f3`; `docs/implementation/evidence/0.6-protocol-schema-foundation.md`; exact 20-file addition; strict TypeScript PASS; 12/12 schema self-validation; 45/45 positive/negative/adversarial fixtures; exact candidate-byte verification PASS; bounded fail-closed validation/no later-phase leakage |
| ↳ **0.7** Typed bootstrap/configuration schemas and machine-readable canonical contract values | **VERIFIED** | 0.6 | PLAN §2; MAN §§4–6; PS §24; CS §30; IC §28 | 10 CA / 9 scoped | None within `0.7` scope | `f6be2c341b628e2906f2fbb2dced9cbb0d60ed73`; `5b2669b26b9f1af0390606420eb23ba372ab999a`; `docs/implementation/evidence/0.7-canonical-contract-values.md`; exact 7-file change (6 added + 1-line export); strict TypeScript PASS; 3/3 schema self-validation; 38/38 positive/negative/adversarial fixtures; 7/7 exact blob matches; dynamic release facts intentionally omitted |
| ↳ **0.8** Pinned toolchains, package manager, strict TypeScript, Rust baseline, lockfiles | **VERIFIED** | 0.2 | PLAN §2; CS §§5–6,28–29; RP §3 | 10 CA / 8 scoped | None within `0.8` repository scope | `6241afc248b9c822ebe0be7319a142e28ae1d2ff`; `6d2afb0c938aa27944909222e8c03e5305613d0f`; `docs/implementation/evidence/0.8-pinned-toolchain-baseline.md`; exact 15-file addition; metadata/lock/schema gate PASS; 5/5 weakening mutations rejected; wrong ambient Node fails closed; 15/15 candidate blobs matched; exact selected binaries intentionally not claimed in this sandbox and remain normal CI/0.CP execution evidence |
| ↳ **0.9** Production test-layer and fixture architecture | **VERIFIED** | 0.6, 0.8 | PLAN §2; CS §27; VR §§6–8 | 10 CA / 9 scoped | None within `0.9` scope | `d9b8f27d09d21e2d65d31dcf010837a0d5135a84`; `cd7a5c0a29774cc50a1c4bbfc6f0051b52ea004c`; `docs/implementation/evidence/0.9-test-layer-architecture.md`; exact 15-path change; normal profile 5 files/9 tests PASS; network/credential isolation PASS; qualification profile fails closed on 10 future empty layers; 15/15 exact candidate blobs matched; no blind rerun-to-green |
| ↳ **0.10** Architecture/forbidden-import/package-cycle/unsafe-Rust-containment/platform-capability-failure enforcement | **VERIFIED** | 0.4, 0.5, 0.9 | PLAN §2; PP §28; CS §§3,28,32; VR §9 | 10 CA / 9 scoped | None within `0.10` scope | `57a26db4d1ff1f129e4ac042eb38eaf4386453c1`; `a979c8560197073e9cb5e4356494f333bd27a02e`; `docs/implementation/evidence/0.10-architecture-enforcement.md`; exact 4-path implementation scope; architecture checker PASS; normal suite 6 files/17 tests PASS; 8/8 architecture mutations/adversarial checks; explicit no-fallback/unsafe/cycle/import enforcement |
| ↳ **0.11** Static CI baseline: format/type/build/schema/security/dependency/license/secret/provenance | **VERIFIED** | 0.8, 0.9 | PLAN §2; CS §§28–29; SEC §§27–28; VR §6 | 10 CA / 9 scoped | None within `0.11` scope | `4cb29e41bdd9ca522a44986f823976aab24f826e`; fixes `df80e90b9a0289ca9b92648c439f431939d05e92`, `ef105dda4f054a36661dde60898facb406910042`; `462336d1b12695b36b8b37cf72f428383cbb6129`; `docs/implementation/evidence/0.11-static-ci-baseline.md`; GitHub Actions run `31589263182` on `ef105dda...` completed `success`; exact toolchain, format, schema, secret, dependency/provenance, strict TS/build, architecture, normal tests, High+ audit, Rust fmt/clippy/host/Windows-target and commit-bound evidence all PASS |
| ↳ **0.12** Generated-artifact reproducibility, manifest validation, and contract/profile drift checks | **VERIFIED** | 0.7, 0.11 | PLAN §2; MAN §6; IC §28; RP §17 | 10 CA / 9 scoped | None within `0.12` scope | `a6043279434824327a3b43e136a47cbd7df7567b`; `31bc81bb17d73391135e62c640680f875b395a16`; `docs/implementation/evidence/0.12-contract-reproducibility-and-drift.md`; exact 13-path change; 15/15 adversarial/structural tests PASS; generated JSON/TS reproducibility + 15-component manifest/revision + contract/profile semantic drift PASS; exact GitHub Actions run `31592319467` on `a6043279...` completed `success` with all inherited 0.11 gates also PASS |
| ↳ **0.13** Authoritative `master` repository governance: server-enforced when available, compensating controls when unavailable | **VERIFIED** | 0.11, 0.12 | PLAN §2; IC §28; RP §17; VR §33; ADR-074 | 10 CA / 8 scoped | None within `0.13` scope; hosting residual risk is explicit and contract-approved only while server protection is unavailable | `8996d6947fd60b415e32485ee3777914f80cd590`; `docs/implementation/evidence/0.13-master-protection-blocker.md`; governance mode `COMPENSATING_CONTROLS`; live `master` truthfully `protected=false`; pre-integration run `31637736770` PASS; non-force authoritative integration PASS; post-integration `master` run `31637928189` PASS; server-side mode becomes mandatory when available |
| ↳ **0.CP** SECTION CHECKPOINT — Phase 0 clean-checkout, governance, platform-boundary, and drift proof | **VERIFIED** | 0.1–0.13 | PLAN §2 Exit; PLAN §26 checkpoint; VR §§6,9,33 | 10 CA / 8 scoped | None | `34a05bd2da4c68583dcfe149efdbaa93a5bc0047`; `docs/implementation/evidence/0.CP-phase0-checkpoint.md`; 12/12 checkpoint adversarial cases PASS; pre-integration run `31640079536` PASS; non-force authoritative integration PASS; post-integration master run `31640209796` PASS; exact Phase-0 CI evidence emitted with all 20 cumulative gates |

### SECTION 1 — Windows Tauri Host / Mission Control Foundation / Application-Owned Core

| Section / Subsection | Status | Depends On | Governing Contract / Traceability | Score | Current Gap | Evidence / Result |
|---|---|---|---|---:|---|---|
| **SECTION 1 — Windows Tauri Host / Mission Control Foundation / Application-Owned Core** | **VERIFIED** | 0.CP | `Phase 1`; checkpoint: Windows Desktop Trust + Mission Control Foundation Ready | 10 CA / 8 scoped | None; later authenticated IPC and full release gates remain owned by subsequent sections | `0.CP` and `1.1–1.14` verified; executable `1.CP` checkpoint PASS; packaged-Core qualification, installer signature verification, fresh standard-user install, 169-file runtime inventory, and responsive installed host all independently PASS |
| ↳ **1.1** Tauri 2 + React desktop workspace with bundled-local authoritative WebView | **VERIFIED** | 0.CP | PLAN §3; RT §§2–5; RP §3; UI §8 | 10 CA / 9 scoped | None within `1.1` scope | Candidate `7981defd48019c13f78f3c1c6d0539cb341bc4fa`; 7/7 `tests/layers/unit/desktop-workspace.test.mjs`; pinned desktop typecheck and bundled frontend build PASS; GitHub run `31668752271` PASS for `windows-desktop` and dependent `static-ci`; native Windows Tauri compile PASS |
| ↳ **1.2** Tauri capabilities, CSP, navigation, external-link, devtools, and inert-content security | **VERIFIED** | 1.1 | PLAN §3; IC §6; RT §5; SEC §20; CS §25; VR §12 | 10 CA / 9 scoped | None within `1.2` scope; native and repository-side evidence complete; mandatory remote CI remains retained but was not triggered for this manual pass | Candidate includes the official pinned opener plugin, narrowly scoped HTTP(S) opener permission, validated external-link helper, restrictive CSP, native authoritative-navigation/new-window/devtools policy, bounded inert renderer text, and 5 adversarial security tests; MSVC-native `cargo check --locked --workspace --target x86_64-pc-windows-msvc` PASS using Visual Studio Community 18.9.12105.275 / MSVC 14.51.36231; local TypeScript typecheck/build PASS; targeted desktop suite 13/13 PASS; normal unit profile 10/10 files PASS; dependency/provenance/schema/architecture checks PASS (`npm=55`, `cargo=470`, 525 approved provenance records); clean LF-normalized worktree format check PASS (`133` files) and Cargo formatting PASS |
| ↳ **1.3** Rust Windows platform host and deterministic Windows composition wiring | **VERIFIED** | 1.1, 0.5 | PLAN §3; IC §§5,8; PP §§7–9; RP §3 | 10 CA / 9 scoped | None within `1.3` scope; concrete Windows capability implementations remain owned by later subsections | Rust host invokes a fail-closed composition gate selecting only `WINDOWS` / `FULL_HOST` / `x64` with profile `windows-v1-x64-full-host`; non-Windows/non-x64 targets return a typed unsupported-target error; native Windows x64 Rust test PASS 1/1; MSVC `cargo check --locked --workspace --target x86_64-pc-windows-msvc` PASS; new-module rustfmt check PASS; composition/architecture tests PASS 9/9; normal unit profile PASS 15/15; Phase-0, architecture, schema, and provenance checks PASS; desktop typecheck/build PASS; no remote CI triggered |
| ↳ **1.4** Single-instance ownership, production data-directory layout, and maintenance-lock foundation | **VERIFIED** | 1.3 | RT §4; UI §9 | 10 CA / 9 scoped | None within `1.4` foundation scope; security descriptor and full recovery qualification remain later gates | Rust host resolves only `%LOCALAPPDATA%\\JARVIS`, creates the required `data`, `backups`, `logs`, `cache`, `artifacts`, `modules`, `updates`, and `recovery` directories after ownership; startup acquires the instance lock before layout completion; maintenance lock has a separate exclusive handle API; native Windows test PASS 4/4 including second-owner rejection and release/reacquisition; MSVC `cargo check --locked --workspace --target x86_64-pc-windows-msvc` PASS with no warnings; lifecycle rustfmt check PASS; architecture, Phase-0, and desktop typecheck checks PASS |
| ↳ **1.5** Application-owned Node/Core packaging, controlled environment, integrity states, and no PATH fallback | **VERIFIED** | 1.3, 0.8 | PLAN §3; IC §7; RT §§3,6; RP §3; VR §§13,32 | 10 CA / 8 scoped | None within 1.5 scope; Tauri/Authenticode, durable anti-rollback, and Job Object launch remain owned by later release/update and 1.7 subsections | Exact application-owned Node 24.18.0/Core release unit is packaged atomically under `resources/core-runtime` with no PATH/system-runtime fallback; controlled environment clears inherited Node modifiers and supplies only explicit Core/TUF paths; native integrity validation rejects missing, traversal, tampered, non-regular, and reparse targets, including support files; Core pre-ready admission verifies local TUF 1.0.35 metadata, signed 2-of-3 root/targets fixture coverage, target hash/length/release identity, and tamper failure; packaged-Core dependency-resolution integration passes; native Windows Rust tests PASS 12/12; normal profile PASS 19 files; Core typecheck/build, bundled frontend build, architecture, schema, secret scan, dependency/provenance, MSVC release check, release Clippy, Phase-0, and governance checks PASS |
| ↳ **1.6** Shared Core bootstrap/service shell with typed UI↔Rust/Core boundary stubs | **VERIFIED** | 1.5, 0.6 | PLAN §§3,22; RT §§10–11; CS §§3,7 | 10 CA / 8 scoped | Authenticated local Core transport and real service round-trip remain owned by Phase 2; this row is limited to the typed, fail-closed shell and boundary stub | Shared protocol types define the locked Windows/FULL_HOST/x64 Core status shape; Core validates exact protocol 1 `get_core_status` requests, UUIDv7 correlation/id, and empty payloads; invalid requests fail with typed validation errors and valid requests remain `CORE_IPC_NOT_READY`/`LOCKED`; Rust/Tauri exposes only the typed `get_core_status` command with deny-unknown-fields input and the same fail-closed behavior; renderer bridge uses `@tauri-apps/api/core` only and runtime-validates success/error response shapes; Core test PASS 4/4, native Windows Rust tests PASS 14/14, strict TypeScript, Core/desktop builds, normal profile PASS 19 files, architecture/schema/secret/dependency/provenance/Phase-0/governance gates PASS |
| ↳ **1.7** Initial PlatformProcessSupervisor Windows Job Object containment for Core startup | **VERIFIED** | 1.5, 0.4 | PLAN §3; RT §§6,9; IC §18 | 10 CA / 8 scoped | Authenticated local IPC/bootstrap integration and worker/module/helper supervision remain owned by later Phase 2 and lifecycle subsections; this row verifies the initial Core containment boundary only | platform/windows/src/process_supervisor.rs implements the Windows-native boundary: integrity-validated CoreLaunchSpec; unnamed owned Job Object with queried JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE and no breakaway flags; CreateProcessW with explicit UTF-16 environment block, bInheritHandles=FALSE, CREATE_SUSPENDED, assign-before-resume, and fail-closed pending cleanup. Native Windows host tests PASS 19/19, including live Core launch through launch_core, queried flags, suspended assignment/resume, kill-on-close termination, and invalid/embedded-NUL launch rejection; release Clippy -D warnings, release check, targeted rustfmt, architecture, normal profile PASS 19 files, strict TypeScript, Core/desktop builds, schema/secret/dependency/provenance/Phase-0/governance gates PASS |
| ↳ **1.8** PlatformWindowController Windows backend, deterministic presentation states, multi-monitor recovery, and conservative no-focus-steal/privacy defaults before the later full NotificationPolicyEngine | **VERIFIED** | 1.3, 0.4 | PLAN §3; UI §§9,24; PP §18 | 10 CA / 8 scoped | Full user-journey, DPI/accessibility, notification-policy, and cross-feature context-preservation qualification remains in later 1.13/1.14 and release verification | `platform/windows/src/window_controller.rs` owns typed `HIDDEN/WINDOWED/MAXIMIZED/FULLSCREEN/FOCUSED_CONTEXT` state, strict bounded logical state persistence, hidden/unfocused/non-topmost startup, explicit-only focus, close-to-hide behavior, monitor selection, and off-screen work-area recovery; native Windows-targeted tests 22/22; desktop security/workspace tests, normal profile 19 files, release check, Clippy `-D warnings`, typecheck/build, architecture/schema/secret/dependency/provenance/Phase-0/governance gates PASS |
| ↳ **1.9** Initial PlatformSessionObserver and PlatformSystemInfo Windows backends | **VERIFIED** | 1.3, 0.4 | PLAN §3; PP §§17,26–27; RP §3 | 10 CA / 8 scoped | Full session-security integration, authoritative Core lock-state routing, notification/privacy suppression, and release support qualification remain later 5.3/1.13/1.14 gates | `platform/windows/src/session_system.rs` implements conservative input-desktop + Terminal Services session observation, explicit `LOCKED/UNLOCKING/UNLOCKED/LOCKING` transitions, validated x64/CPU/memory/uptime/remote-session facts, stable sanitized platform errors, and explicit unqualified hardware acceleration; native Windows tests 27/27 including live session/system probes; normal profile 19 files, release check, Clippy `-D warnings`, typecheck/build, architecture/schema/secret/dependency/provenance/Phase-0/governance gates PASS |
| ↳ **1.10** Initial PlatformPathsAndIdentity Windows application/data path backend | **VERIFIED** | 1.3, 0.4 | PLAN §3; PP §13; CS §16 | 10 CA / 8 scoped | Full project-target canonicalization remains owned by later 6.2; clean-install and release qualification remain later 1.14 gates | `platform/windows/src/path_identity.rs` provides fixed `%LOCALAPPDATA%\JARVIS` resolution, absolute Drive-root enforcement, parent/NUL rejection, reparse-component rejection, case-insensitive identity keys, and typed fail-closed path errors; composition root wiring and negative/live path tests added; native Windows tests 30/30; release Clippy and locked Windows-target cargo check PASS; normal profile 19 files, architecture, typecheck, web build, Phase 0, governance, schema, secret, dependency, and provenance gates PASS |
| ↳ **1.11** Canonical brand assets, offline Inter packaging, and asset/license provenance foundation | **VERIFIED** | 1.1 | PLAN §3; IC §26; UI §§3–5; `assets/brand/README.md`; RP §3 | 10 CA / 8 scoped | Final release-time installer/PNG/ICO qualification remains owned by later 18.5 and Section 1.14; current work establishes canonical source wiring and offline package inputs | Canonical SVG sources are packaged through Vite’s repository asset directory; Inter v4.1 variable WOFF2 and SIL OFL 1.1 notice are local; deterministic `tools/assets/generate-brand-icons.mjs` derives the Windows ICO; machine-readable SHA-256 provenance is included; focused asset/provenance tests PASS; full normal profile 19 files, typecheck/build, architecture, schema, secret, dependency, provenance, Phase 0, governance, release Clippy, and locked Windows-target cargo check PASS |
| ↳ **1.12** Central design tokens, reusable components, keyboard/focus, reduced-motion, and forced-colors foundation | **VERIFIED** | 1.11 | PLAN §3; UI §§6–7,18,20,22–23; CS §25 | 10 CA / 8 scoped | Later 1.13/1.14 and Section 1 checkpoint still own full Mission Control shell, viewport/DPI, and end-to-end accessibility qualification | Central token groups cover brand/surface/text/border/status/spacing/radius/typography/motion/focus/z-order; reusable `SkipLink`, `Button`, `Panel`, `TextInput`, and `StatusChip` primitives provide semantic names/roles, 24px minimum targets, visible focus, keyboard-safe button defaults, error/status announcements, reduced-motion suppression, and forced-colors adaptation; focused suite 9/9, normal profile 19 files, typecheck/build, architecture, schema, secret, dependency, provenance, Phase 0, governance, release Clippy, and locked Windows-target cargo check PASS |
| ↳ **1.13** Mission Control shell, locked startup surface, and truthful foundational states | **VERIFIED** | 1.6, 1.8, 1.12 | PLAN §3; IC §§4,6; UI §§8,11,17,25; OPS §§2–3 | 10 CA / 8 scoped | Full Core-authenticated read model, native show/hide/focus integration, and complete user-journey/accessibility qualification remain later Phase 2/1.14/release gates | Four-region Mission Control shell provides navigation rail, status strip, primary workspace, and context/attention pane over shared primitives; locked startup truthfully shows `LOCKED` / `NOT_CONNECTED` with no fabricated mission data; initial shell remains presentation-only; focused tests 15/15 and normal profile 19 files PASS; typecheck/build, architecture, schema, secret, provenance, governance, release Clippy, and locked Windows-target cargo check PASS |
| ↳ **1.14** Clean Windows 11 x64 standard-non-admin desktop foundation proof: packaged Core, window state, repair/degraded paths, no system Node | **VERIFIED** | 1.2–1.13 | PLAN §3 Exit; VR §§11–13,32 | 10 CA / 8 scoped | None within the scoped Windows foundation proof; authenticated IPC and full release qualification remain owned by Phase 2 and later release sections; the local personal certificate is self-signed and trusted only in the current user's stores, so public production trust is not claimed | Exact source `1ecfc77ad65487b5459fbe78a0240e0d5b922707` passed `release:qualify-core-runtime` using the real Windows x64 Node 24.18.0 binary: packaged Node reported `v24.18.0`, TUF 1.0.35 admission and release sequence 1 passed, packaged Core stayed alive until a controlled stop, and stdout/stderr were empty; Tauri CLI `2.11.5` rebuilt NSIS and MSI with Windows signing configured through certificate thumbprint `23DA4DA3E340B66EC4240B4CC845E4387E5BBDD3`; both fresh installer signatures verify with `signtool /pa`, and a fresh standard-user silent NSIS install exited 0, contains all 169 packaged runtime files, contains a validly signed installed host, and the host remains responsive with a live desktop window; direct release qualification passed; normal repository profile passed all 20 files; focused desktop workspace tests pass 10/10 and root typecheck passes; typecheck, desktop web build, architecture, schema, dependency, provenance, governance, and release Clippy (`-D warnings`) gates all passed; the local certificate is self-signed and has no trusted public timestamp, so it is suitable for this single-user installation but is not a public production trust claim; missing/corrupt verified runtime opens a truthful `REPAIR_REQUIRED` surface instead of silently falling back or aborting before UI; the native validator models and validates the emitted source/release/TUF identity fields, focused Windows runtime tests pass 8/8, and a fresh release host builds |
| ↳ **1.CP** SECTION CHECKPOINT — Windows desktop trust and Mission Control foundation | **VERIFIED** | 1.1–1.14 | PLAN §3 Exit; PLAN §26 checkpoint; VR §§9,11–13,32 | 10 CA / 8 scoped | None within the Phase 1 foundation checkpoint; named-pipe authentication and later full-release qualification remain downstream gates | `tools/checkpoints/phase1-checkpoint.mjs` PASS with negative child-row and runtime-identity tests; packaged-Core qualification PASS (`v24.18.0`, TUF `1.0.35`, release sequence `1`, controlled stop); NSIS/MSI `signtool /pa` PASS; fresh standard-user NSIS install exit `0`, 169 packaged runtime files, installed host signature `Valid`, and responsive window handle; normal suite 21 files, typecheck, and architecture checks PASS |

### SECTION 2 — Platform Local IPC / Process / Privilege Boundaries

| Section / Subsection | Status | Depends On | Governing Contract / Traceability | Score | Current Gap | Evidence / Result |
|---|---|---|---|---:|---|---|
| **SECTION 2 — Platform Local IPC / Process / Privilege Boundaries** | **VERIFIED** | 1.CP | `Phase 2`; checkpoint: Phase 2 IPC / Process / Privilege Boundary Exit | 10 CA / 8 scoped | None within the completed Section 2 boundary; current-source release bytes still require later signed TUF release qualification | Section 1 checkpoint is verified; 2.1–2.9 and 2.CP are verified. The integrated checkpoint passed native IPC/authentication and locked-status round-trip tests, alternate-principal denial, Job Object/process-tree containment, narrow privilege-mediator fail-closed behavior, architecture checks, strict Clippy, TypeScript, and the normal 22-file profile; `legion\\test account` was denied by the actual repository-owned named-pipe fixture. |
| ↳ **2.1** Windows PlatformLocalIpc named-pipe endpoint, restrictive DACL, session principal, local-only semantics | **VERIFIED** | 1.CP, 0.4 | PLAN §4; RT §7; SEC §19; PP §15; VR §13 | 10 CA / 8 scoped | None within the scoped native endpoint boundary; bootstrap authentication and framed Core protocol remain owned by 2.2–2.3 | `platform/windows/src/local_ipc.rs` creates an unpredictable per-launch `\\.\pipe\jarvis-core-*` endpoint from `BCryptGenRandom`, applies an explicit protected SID-only DACL for the current authenticated user using the narrowed `0x0012019F` exchange mask rather than `GA/FILE_ALL_ACCESS`, rejects remote clients with `PIPE_REJECT_REMOTE_CLIENTS`, and rejects clients from a different Windows session; `main.rs` owns the endpoint during Windows host startup; native Windows local-IPC tests PASS 2/2 (endpoint entropy/ACL/local-only assertions and same-session client acceptance); locked Windows-target cargo check PASS; release Clippy `-D warnings` PASS; full native Rust suite PASS 46/46 with 1 ignored external fixture; architecture PASS; normal repository profile PASS 21 files |
| ↳ **2.2** Bootstrap-secret generation/secure transfer, authenticated handshake, bounded framing, protocol-major negotiation | **VERIFIED** | 2.1, 0.6 | PLAN §4; RT §§6–7; PS §7; VR §§10,13 | 10 CA / 8 scoped | Live alternate-session qualification remains owned by 2.3; full process lifecycle/cancellation and typed Core round-trip remain downstream | `platform/windows/src/local_ipc.rs` generates a per-launch CSPRNG secret and endpoint, transfers bootstrap material through an inherited anonymous pipe selected by `PROC_THREAD_ATTRIBUTE_HANDLE_LIST` (never command line/environment/logs), performs bounded 1 MiB little-endian framed JSON challenge/hello/welcome negotiation at protocol major `1`, enforces exact Windows session/DACL/local-only endpoint checks, and fails closed on timeout, malformed/oversized frames, protocol mismatch, or wrong proof; `services/core/src/ipc-bootstrap.ts` validates the bounded bootstrap frame and performs the matching named-pipe HMAC handshake; `main.rs` starts the verified Core under the Job Object before retaining the authenticated host runtime; native Windows tests PASS 46/46, qualification-profile Core IPC tests PASS 3/3, normal repository profile PASS 22 files, architecture PASS, Core typecheck/build PASS, release Clippy `-D warnings` PASS, locked release host build PASS, and supervised packaged Core qualification PASS with Node `v24.18.0`, TUF `1.0.35`, authenticated transport `true`, protocol major `1`, and controlled stop `true`; the direct release GUI host remains alive beyond the 5-second handshake deadline |
| ↳ **2.3** IPC adversarial isolation: wrong principal/session/remote/secret, renderer denial, malformed/oversized/protocol mismatch | **VERIFIED** | 2.2 | PLAN §4 Exit; SEC §§19,33; VR §13 | 10 CA / 8 scoped | None within the scoped IPC adversarial-isolation boundary; same-user compromise protection remains outside this subsection as defined by ADR-056 | Native Windows tests PASS 46/46, including bounded accept cancellation, wrong bootstrap secret, malformed/oversized frames, protocol mismatch, same-session acceptance, restricted-principal SID-only-DACL denial, and rejection of a remote named-pipe path; the normal native suite passes 46 with 1 explicitly ignored external qualification fixture; `tests/layers/unit/desktop-security.test.mjs` passes renderer direct-channel denial checks; normal profile PASS (22 files), direct qualification-profile Core IPC PASS 3/3, architecture/typecheck/strict Clippy PASS; supervised packaged Core qualification passes and the actual release GUI host remains alive beyond the handshake deadline; separate-account standalone protected-pipe probe PASS and repository-owned actual-code `external_principal_qualification_fixture` denied `legion\\test account` access (`PASS: JARVIS denied access`) |
| ↳ **2.4** Full PlatformProcessSupervisor Windows process-tree ownership, suspended assignment, handle allowlist, kill-on-close | **VERIFIED** | 1.7 | PLAN §4; RT §9; PP §16; CS §10; VR §19 | 10 CA / 8 scoped | Typed lifecycle/protocol cancellation requests remain owned by higher layers; provider/worker/module/helper supervision remains downstream | `platform/windows/src/process_supervisor.rs` creates an explicitly owned Windows Job Object with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`, leaves both breakaway flags disabled, creates children suspended, assigns them before `ResumeThread`, blocks launch on containment failure, and uses `PROC_THREAD_ATTRIBUTE_HANDLE_LIST` for the exact bootstrap/stdout/stderr inheritance set while normal launches inherit none; native Windows supervisor tests PASS 7/7, including Job Object flags, suspended assignment, integrity-validated Core launch, explicit bootstrap-handle transfer, invalid-spec fail-closed behavior, direct member kill-on-close, and a real Toolhelp-discovered `ping.exe` descendant terminated by Job Object close; strict Windows-target Clippy `-D warnings` PASS; `git diff --check` PASS |
| ↳ **2.5** Managed-process cancellation escalation, forced shutdown, descendant/orphan cleanup, and truthful containment diagnostics | **VERIFIED** | 2.4 | PLAN §4; RT §§9,23; PP §§16,27; VR §19 | 10 CA / 8 scoped | None within the native supervisor boundary; the owning lifecycle layer must issue the semantic cooperative request, while this boundary preserves the typed reason, grace deadline, forced Job Object termination, and verification | `SupervisedCoreProcess::shutdown_after_cooperative_request` carries typed reasons (`UserCancel`, `Timeout`, `ProviderFailure`, `ProviderSetupRepair`, `PausePreemption`, `ProcessTermination`, `AppShutdown`), waits a bounded grace period, force-terminates the complete Job Object on timeout, waits for the root process to signal, verifies the Job Object active-process count reaches zero, and exposes `ProcessContainmentDiagnostics` without human-text parsing; native Windows supervisor tests PASS 8/8, including graceful shutdown, forced shutdown, descendant termination, empty-Job-Object verification, and direct member cleanup; full Windows-target Rust suite PASS 48/48 with 1 explicitly ignored external IPC fixture; strict Clippy `-D warnings`, targeted rustfmt, and `git diff --check` PASS |
| ↳ **2.6** PlatformPrivilegeMediator bounded operation registry and Windows UAC/elevation boundary | **VERIFIED** | 1.CP, 0.4 | PLAN §4; RT §8; SEC §22; PP §9 | 10 CA / 8 scoped | Actual provider UAC launch remains intentionally unqualified until subsection 7.5; no normal worker receives elevation | `platform/windows/src/privilege_mediator.rs` exposes only `PROVIDER_SETUP_REPAIR`, rejects unknown operation identities and NUL/oversized arguments, binds the mediator to its own registry, requires an absolute installed helper identity and re-hashes it before any future launch, and returns truthful unavailable/unqualified errors without invoking UAC; native privilege tests PASS 5/5, including changed-helper rejection before the UAC lifecycle; full Windows-target Rust suite PASS 53/53 with 1 explicitly ignored external IPC fixture; strict Clippy `-D warnings`, targeted rustfmt, and `git diff --check` PASS |
| ↳ **2.7** Native broker capability surface: typed secure/process/window/session/update/audio operations; no generic command broker | **VERIFIED** | 2.4, 2.6 | RT §8; IC §8; SEC §§6,22 | 10 CA / 8 scoped | Concrete secure-storage, updater, audio, and provider setup operations remain owned by their later qualification subsections; this broker reports them unavailable/unqualified and does not claim support | `platform/windows/src/native_broker.rs` composes semantic `NativeCapabilityId`/status values and typed session observation without exposing Windows handles, executable paths, command strings, or provider-native objects; verified process/window backends are represented as qualified, live session status is mapped from the existing conservative observer, and secure storage/update/audio/privilege remain truthful unavailable/unqualified; native broker tests PASS 3/3; full Windows-target Rust suite PASS 56/56 with 1 explicitly ignored external IPC fixture; strict Clippy `-D warnings`, targeted rustfmt, and `git diff --check` PASS |
| ↳ **2.8** Pre-persistence bootstrap ordering and fail-closed diagnostics through authenticated Core IPC: implement the Phase-2-available portions of the Runtime bootstrap sequence, register the typed PlatformSecureStorage boundary without claiming the Phase-3 Windows secure-store backend, and leave persistence/recovery/provider startup stages explicitly unqualified until their owning sections | **VERIFIED** | 2.2, 2.5, 2.7 | PLAN §§4,22; RT §6 sequencing; OPS §§2,7,24; VR §13 | 10 CA / 8 scoped | Persistence, recovery, provider discovery, and the real Windows secure-store implementation remain intentionally unqualified for their later owning sections; the current host shows `DEGRADED` and does not start Core unless all mandatory pre-Core conditions are `LOCKED` | `apps/desktop/src-tauri/src/lifecycle.rs` adds an ordered bootstrap ledger for ownership → diagnostics → locked session → configuration → secure-storage registration → runtime integrity → IPC/secret/containment/authentication, rejects out-of-order transitions, and flushes bounded non-secret lifecycle/failure records to `bootstrap.log`; `platform/windows/src/native_broker.rs` registers the typed Windows secure-storage boundary as unavailable/unqualified; `main.rs` records the sequence, maps missing secure storage to `DEGRADED`, maps runtime preflight failure to `REPAIR_REQUIRED`, and prevents Core startup unless the condition is exactly `LOCKED`; lifecycle/native-broker/main tests and full Windows-target Rust suite PASS 59/59 with 1 explicitly ignored external IPC fixture; release Windows-target check, strict Clippy `-D warnings`, targeted rustfmt, `git diff --check`, repository normal profile PASS 22 files, architecture PASS, and TypeScript typecheck PASS; repository-wide format command remains affected by pre-existing CRLF content across the Windows checkout and is not treated as evidence for this scoped change |
| ↳ **2.9** Pre-autonomy control-plane slice: deterministic LOCKED Core state, minimal state/event ownership interfaces, and harmless get_system_status round-trip (not the later general ToolExecutor) | **VERIFIED** | 2.8, 1.13 | PLAN §22 First Implementation Slice; RT §§10–12; PS §§8–9 | 10 CA / 8 scoped | Production release qualification still requires a newly signed TUF metadata set for the changed Core bytes; persistence, recovery, and general tool ownership remain downstream | `services/core/src/main.ts` now serves only the authenticated bounded Core status request and keeps the result `LOCKED` / `NOT_CONNECTED`; `tests/layers/unit/core-bootstrap.test.mjs` passes 6/6, including the framed authenticated transport round-trip and unknown-request rejection; `platform/windows/src/local_ipc.rs` sends and validates the exact `protocolMajor: 1` request/response over the authenticated named pipe, with native round-trip evidence; full Windows Rust suite PASS 59/59 with 1 explicitly ignored external-account fixture, release check PASS, strict Clippy PASS, TypeScript PASS, `git diff --check` PASS, and normal profile PASS 22 files. Current-source release bytes were rebuilt separately, but the existing signed TUF fixture was preserved for qualified package tests rather than misrepresented as a newly signed release. |
| ↳ **2.CP** SECTION CHECKPOINT — intended Core connects; unauthorized peers fail; process/elevation boundaries cannot be bypassed | **VERIFIED** | 2.1–2.9 | PLAN §4 Exit; VR §§13,19 | 10 CA / 8 scoped | None within the completed Section 2 checkpoint; signed release-artifact qualification remains a later release gate | Focused checkpoint evidence PASS: native IPC group 15/15 with 1 ignored external fixture, including authenticated native Core status round-trip, wrong secret/session/remote/malformed/oversized/protocol rejection, and packaged/supervised Core authentication; process-supervisor group 8/8 including Job Object descendant cleanup and graceful/forced shutdown; privilege-mediator group 5/5 including changed-helper rejection and no generic elevation; architecture check PASS; full Rust 59/59, normal profile 22 files, TypeScript, release check, strict Clippy, and `git diff --check` PASS. |

### SECTION 3 — Persistence / Secure Storage / KDF / JARVIS_BACKUP_V1 Proof

| Section / Subsection | Status | Depends On | Governing Contract / Traceability | Score | Current Gap | Evidence / Result |
|---|---|---|---|---:|---|---|
| **SECTION 3 — Persistence / Secure Storage / KDF / JARVIS_BACKUP_V1 Proof** | **VERIFIED** | 2.CP | `Phase 3`; checkpoint: Persistence/KDF/JARVIS_BACKUP_V1 Portable Recovery Proven | 10 CA / 8 scoped | None within Section 3; later release/profile gates remain outside this section | Required 3.1–3.12 and 3.14–3.18 are verified; optional 3.13 remains contract-deferred; the dedicated `3.CP` checkpoint passed after qualification-profile, packaged restore, cross-language vector, native Windows, typecheck, dependency/provenance, and secret-scan evidence were reconciled |
| ↳ **3.1** Select/pin production SQLite/SQLCipher binding and prove exact embedded WAL-reset-fix identity | **VERIFIED** | 2.CP, 0.8 | PLAN §5; IC §20; RP §4; DS §2; VR §20 | 10 CA / 8 scoped | The binding identity is fixed, but encrypted DB lifecycle, secure DB_DEK storage, snapshot/re-key, and full persistence qualification remain owned by 3.2–3.18 | Pinned `better-sqlite3-multiple-ciphers@12.11.1` with approved provenance and lockfile entry; packaged Core includes the binding and excludes install-only `prebuild-install`; direct Windows x64 binding probe and `tests/layers/unit/sqlite-binding.test.mjs` PASS 1/1; embedded SQLite reports `3.53.2`, source id `2026-06-03 19:12:13 d6e03d8c777cfa2d35e3b60d8ec3e0187f3e9f99d8e2ee9cac695fd6fcdf1a24`, `COMPILER=msvc-1944`, `THREADSAFE=2`; file-backed probe reports `journal_mode=wal`, `synchronous=2/FULL`, and `foreign_keys=1`; upstream SQLite WAL documentation and the 3.51.3 release log identify the reset fix that is included by this later embedded version; provenance/dependency checks PASS, packaged-runtime test PASS, normal profile PASS 23 files, Core and desktop TypeScript typecheck PASS, and `git diff --check` PASS; current changed release bytes still require later newly signed TUF qualification |
| ↳ **3.2** Owned DB connection factory: qualified local path, WAL, FULL synchronous, foreign keys, bounded busy/checkpoint diagnostics | **VERIFIED** | 3.1, 1.10 | PLAN §5; DS §§2,26; CS §13; VR §20 | 10 CA / 8 scoped | Encrypted SQLCipher DB lifecycle, secure DB_DEK protection, full packaged qualification, online snapshot/re-key, and later recovery semantics remain owned by 3.4–3.18 | `services/core/src/persistence.ts` owns the connection initialization path, rejects relative/UNC/device/unready paths, pins the qualified binding identity, establishes and verifies file-backed WAL, `synchronous=FULL`, `foreign_keys=ON`, and bounded `busy_timeout`; exposes typed PASSIVE/FULL/RESTART/TRUNCATE checkpoint diagnostics and complete/incomplete status; contention is bounded and reopen preserves committed state; `tests/layers/unit/core-persistence.test.mjs` passes 3/3 including negative path admission and SQLITE_BUSY behavior; normal profile PASS 24 files, full typecheck, architecture, provenance, dependency, and `git diff --check` PASS |
| ↳ **3.3** Minimal proof schema/migration and transactional state+causative-event skeleton, including only recovery-compatible credential-handle/integration metadata needed by Phase-3 restore semantics | **VERIFIED** | 3.2, 0.6 | PLAN §§5,22; DS §§5–8,34; CS §§13–14 | 10 CA / 8 scoped | Full production migration backup/lock/recovery lifecycle, complete domain schema, state-machine ownership, deduplication, and packaged persistence qualification remain downstream | `services/core/src/schema.ts` provides deterministic migration `0001-core-proof-schema` with SHA-256 identity, schema-version rejection, integrity/foreign-key validation, optimistic versioned `authoritative_records`, append-only causative `events`, and restore-safe `integration_accounts` with opaque `credential_handle` plus `REAUTH_REQUIRED`; `tests/layers/unit/core-schema.test.mjs` passes 3/3, including idempotent migration/newer-schema rejection, atomic state+event rollback on stale/duplicate conflict, and credential-field negative checks; normal profile PASS 25 files, full typecheck, architecture, and `git diff --check` PASS |
| ↳ **3.4** PlatformSecureStorage Windows backend and random local DB_DEK lifecycle using opaque handles | **VERIFIED** | 2.7, 3.2 | PLAN §5; PP §14; SEC §§6–7; DS §3; RP §4 | 10 CA / 8 scoped | Core/DB integration, SQLCipher key application, clean-profile restore re-key, broader credential-broker scope controls, and packaged backup qualification remain downstream | `platform/windows/src/secure_storage.rs` implements the typed current-user DPAPI boundary, CSPRNG-generated 256-bit DB_DEK, strict opaque `dpapi-v1-*` handle, reparse/path identity checks, atomic protected-blob publication, transient `DatabaseDek` zeroization on drop, open/rotate/delete lifecycle, and bounded protected-blob validation; `native_broker.rs` exposes only typed secure-storage operations and reports availability/qualification truthfully; desktop startup registers the backend from the per-user JARVIS data root; Windows-target native tests PASS 62/62 with 1 separately ignored external-account fixture, including actual DPAPI round-trip and rotation; release Cargo check and strict Clippy `-D warnings` PASS; full TypeScript typecheck, architecture, and `git diff --check` PASS |
| ↳ **3.5** Production KDF profile schemas/Argon2id path, parameter bounds, OS-CSPRNG salts, and under-floor rejection | **VERIFIED** | 3.3, 3.4 | PLAN §5; IC §9; PS §6; DS §5; SEC §4; CS §15; VR §14 | 10 CA / 8 scoped | Full DB_DEK-to-SQLCipher integration, password/recovery verifier persistence, backup-slot wiring, and release qualification remain downstream | `packages/protocol/src/kdf.ts` and the canonical/schema artifacts retain the exact Argon2id v0x13 profile contract: session/portable-recovery floor 65,536 KiB, 3 iterations, parallelism 4, 16-byte salt, 32-byte output; portable-backup-passphrase floor 262,144 KiB. `platform/windows/src/kdf.rs` provides versioned session, portable-recovery, and stronger portable-backup profiles; validates profile IDs and schema/resource bounds before derivation; rejects under-floor, wrong-version, wrong-parallelism, invalid salt/output, and over-ceiling parameters; generates salts with Windows `BCryptGenRandom`; derives with RustCrypto `argon2` 0.5.3 Argon2id v0x13. Native target tests PASS 66/66 with 1 separately ignored external-account fixture, including deterministic salt-bound derivation, random-salt checks, exact-floor profiles, and negative under-floor/unsafe-profile cases; strict Clippy `-D warnings`, release Cargo check, full TypeScript typecheck, architecture, dependency/provenance checks, normal profile PASS 25 files, and `git diff --check` PASS |
| ↳ **3.6** Encrypted SQLCipher DB create/open, WAL crash/restart, transaction and corruption/integrity proof | **VERIFIED** | 3.2–3.5 | PLAN §5 Required proof; DS §§2,26,35; VR §20 | 10 CA / 8 scoped | Core startup DB_DEK provisioning integration and final signed-release persistence qualification remain | `services/core/src/persistence.ts` requires a transient exact 32-byte `DB_DEK`, applies the release-qualified `cipher='sqlcipher'` + legacy `4` profile through `better-sqlite3-multiple-ciphers`, verifies WAL/FULL/FK/bounded busy settings, verifies cipher/build identity, and performs SQLite integrity plus foreign-key checks. Typed failures distinguish missing/invalid DB_DEK and integrity failure without deleting the damaged database. Focused persistence tests PASS 7/7, including encrypted on-disk bytes, wrong-key rejection, ungraceful child-process termination with WAL present and 200-row recovery after reopen, and typed integrity failure preservation. Packaged-runtime qualification imports the packaged persistence/schema modules in an isolated child process, applies migrations, verifies integrity and SQLCipher identity, and cleans the release tree successfully. Full normal profile PASS 25 files; Core build/typecheck, repository typecheck, architecture, dependency, provenance, and `git diff --check` PASS. |
| ↳ **3.7** Exact SQLCipher-safe snapshot/re-key/export mechanism under fresh SnapshotDBKey | **VERIFIED** | 3.6 | PLAN §5; BKC §4; RP §4; VR §§20–21 | 10 CA / 8 scoped | Core startup integration and final signed-release persistence qualification remain | `services/core/src/persistence.ts` implements the release-qualified `attached-sqlcipher-schema-export-v1` path: it rejects active transactions and unqualified/overwrite destinations, attaches a fresh exact 32-byte `SnapshotDBKey` without exposing it in metadata, copies the complete supported table/index/view/trigger schema and rows inside `BEGIN IMMEDIATE` with deferred foreign-key validation, commits before detach, publishes atomically through a staging path, and reopens the snapshot read-only under the independent key with DELETE journal mode and integrity checks. The live database remains WAL. `SqlcipherSnapshotConnection` rejects wrong keys, malformed key lengths, non-integrity snapshots, unsupported virtual/temporary schema, and failed cleanup. Focused persistence tests PASS 8/8, including schema/data/index/view/trigger preservation, independent-key open, wrong-key/overwrite/active-transaction rejection, encrypted bytes, and sidecar/staging cleanup. The packaged Core test exercises the compiled exporter/opener and passes. The release runtime manifest now records binding/version, SQLCipher profile, SQLite/source identity, upstream WAL-reset-fix evidence, and exact snapshot mechanism derived from the compiled persistence module. Full normal profile PASS 25 files; Core build/typecheck, repository typecheck, architecture, dependency, provenance, and `git diff --check` PASS. |
| ↳ **3.8** JARVIS_BACKUP_V1 bounded descriptor/parser and RFC8785 descriptor digest | **VERIFIED** | 3.5, 0.7 | BKC §§2,5; PLAN §5; CS §26 | 10 CA / 8 scoped | Outer payload/key-slot authentication and full backup package qualification remain owned by 3.9–3.18 | `services/core/src/backup-descriptor.ts` defines the exact V1 descriptor field set, bounded UTF-8 input, strict UUIDv7/UTC timestamp/base64url validation, canonical uint64 and chunk-count/1 TiB checks, exact 16-slot limit, RFC 8785-compatible canonical JSON bytes, and SHA-256 descriptor digest. Noncanonical whitespace/order, duplicate keys, malformed UTF-8/JSON, unsupported identity, bad bounds, and inconsistent chunk counts fail closed. The compiled `backup-descriptor.js` module is included in the release-owned Core support set and is checked in packaged output. Focused descriptor tests PASS 3/3; manifest/package/packaged tests PASS 10/10; full normal profile PASS 26 files; Core build/typecheck, repository typecheck, architecture, dependency, provenance, and `git diff --check` PASS. |
| ↳ **3.9** Backup key hierarchy, fresh BackupDEK/SnapshotDBKey generation, authenticated payload manifest, secret exclusion | **VERIFIED** | 3.7, 3.8 | BKC §§3,11; IC §21; DS §§27,29 | 10 CA / 8 scoped | Outer AES-GCM chunk encryption and key-slot wrapping remain owned by 3.10–3.12; this subsection proves only fresh transient key material and the bounded manifest boundary | `services/core/src/backup-manifest.ts` creates independent fresh 256-bit `SnapshotDBKey` and `BackupDEK` values from Node's OS-backed CSPRNG path, rejects the impossible equal-key result by regenerating, and provides explicit zeroization. `buildBackupPayloadManifest` constructs bounded `jarvis.backup.payload-manifest.v1` metadata with exact V1 identity, protection/version metadata, internal-only SnapshotDBKey reference, sorted unique logical objects, canonical relative paths, canonical uint64 sizes, base64url SHA-256 identities, and non-secret key-slot profile metadata; secret-bearing paths/recovery material, duplicate paths, malformed digests/quantities, unknown fields, and over-bounds fail closed. The canonical JCS bytes and SHA-256 digest are the manifest-authentication input; neither key is serialized or returned in manifest metadata. Focused backup descriptor/manifest tests PASS 6/6; release manifest/package/packaged support tests PASS 7/7; full normal profile PASS 27 files; Core build/typecheck, repository typecheck, architecture, dependency, provenance, secret scan, and `git diff --check` PASS. Repository format check remains blocked by pre-existing CRLF/final-newline violations across the Windows checkout and is not treated as evidence for this scoped change. |
| ↳ **3.10** AES-256-GCM 4 MiB chunk framing, nonce/AAD, count/order/truncation/duplicate/append authentication | **VERIFIED** | 3.9 | BKC §6; PLAN §5; VR §21 | 10 CA / 8 scoped | Whole-package streaming/container integration and key-slot authentication remain downstream; this subsection proves the exact chunk cryptographic boundary | `services/core/src/backup-chunks.ts` implements AES-256-GCM with exact 4 MiB chunking, one fresh 4-byte nonce prefix per backup, big-endian uint64 chunk indices, and RFC 8785-compatible JCS AAD containing the exact backup/chunk identity, descriptor digest, count, and plaintext length. Decryption requires contiguous zero-based records, exact full/final lengths, exact total length, and authenticates every chunk before returning plaintext. Focused chunk tests PASS 3/3, including deterministic fixed-input vectors, round-trip, wrong-key/tamper failure, reordering, duplication, truncation, append, key/digest/nonce mismatch, and descriptor/count rejection; release manifest/package/packaged support tests PASS 7/7; full normal profile PASS 28 files; Core build/typecheck, repository typecheck, architecture, dependency, provenance, secret scan, and `git diff --check` PASS. Repository format check remains blocked by pre-existing CRLF/final-newline violations across the Windows checkout and is not treated as evidence for this scoped change. |
| ↳ **3.11** GENERATED_RECOVERY_V1 256-bit factor representation, HKDF slot KEK, wrap-AEAD, and export/privacy constraints | **VERIFIED** | 3.10 | BKC §§7–8; IC §21; RP §4 | 10 CA / 8 scoped | Optional passphrase slots, Windows local-recovery slots, and full package/restore integration remain downstream; this subsection proves only the mandatory generated portable-recovery factor and slot cryptographic boundary | `services/core/src/backup-recovery.ts` implements the exact `JRV1-` plus 32-byte unpadded-base64url representation, transient CSPRNG generation and explicit clearing, HKDF-SHA-256 slot KEK derivation with `recoverySecret` as IKM, descriptor digest as salt, and exact generated-recovery info prefix plus slot ID. It wraps only the 256-bit `BackupDEK` with AES-256-GCM using a fresh 96-bit nonce, 128-bit tag, and JCS AAD containing the exact key-slot identity, backup ID, descriptor digest, slot ID, slot type, and wrap algorithm; slot records contain no recovery secret or KEK. Focused generated-recovery tests PASS 3/3, including representation bounds, round-trip unwrap, wrong-factor failure, and metadata/tamper rejection; release manifest/package/packaged support tests PASS 7/7; full normal profile PASS 29 files; Core build/typecheck, repository typecheck, architecture, dependency, provenance, secret scan, and `git diff --check` PASS. Repository format check remains blocked by pre-existing CRLF/final-newline violations across the Windows checkout and is not treated as evidence for this scoped change. |
| ↳ **3.12** Windows LOCAL_RECOVERY DPAPI/PlatformSecureStorage BackupDEK slot | **VERIFIED** | 3.9, 3.4 | BKC §10; DS §32; RP §4 | 10 CA / 8 scoped | Whole-package slot-record serialization, package verification, restore integration, and explicit non-portable labeling remain owned by 3.14–3.16; this subsection proves the qualified Windows current-user secure-storage boundary | `platform/windows/src/secure_storage.rs` adds a distinct transient `BackupDek` role and DPAPI protect/unprotect operations that require non-empty bounded caller-supplied additional entropy, allowing the owning backup layer to bind the protected `BackupDEK` to exact backup/descriptor identity without storing key material in ordinary state. `native_broker.rs` exposes only typed `protect_backup_dek`/`unprotect_backup_dek` operations; DB_DEK and BackupDEK types remain distinct and zeroizing. Windows native tests PASS 6/6, including actual current-user DPAPI BackupDEK round-trip, wrong-context rejection, empty-entropy rejection, and existing DB_DEK lifecycle; targeted rustfmt, strict Clippy `-D warnings`, repository typecheck, architecture, secret scan, and `git diff --check` PASS. Repository-wide formatter remains affected by pre-existing CRLF/final-newline violations across the Windows checkout and is not treated as evidence for this scoped change. |
| ↳ **3.13** Optional PASSPHRASE_ARGON2ID_V1 backup slot — implement only if enabled by product/release configuration | **DEFERRED** | 3.11 | BKC §9; RP §4; PLAN §5 | — | Contract-permitted optional capability; not required for base V1 Production Complete unless enabled | Must be fully qualified before any release exposes it |
| ↳ **3.14** Whole-package verification: bounded parse, slot authentication, manifest/hash, SnapshotDBKey open, SQLite integrity | **VERIFIED** | 3.10–3.12; +3.13 if enabled | BKC §§11–14; DS §30; VR §21 | 10 CA / 8 scoped | Clean-profile restore, fresh DB_DEK re-key, reconciliation, and later cross-language/release qualification remain owned by 3.15–3.18 | `services/core/src/backup-manifest.ts` strictly parses bounded canonical decrypted-manifest bytes and requires `GENERATED_RECOVERY_V1` for `PORTABLE_STATE`; `services/core/src/backup-payload.ts` provides a bounded authenticated payload envelope that wraps `SnapshotDBKey` under `BackupDEK`, binds the descriptor digest, verifies canonical manifest/object metadata, exact object order and sizes, SHA-256 content identities, and rejects trailing bytes; `services/core/src/backup-package.ts` constructs and verifies the descriptor, generated-recovery slot, authenticated chunks, payload binding, recovered `SnapshotDBKey`, and SQLCipher snapshot integrity without returning `BackupDEK`. The live package test creates an encrypted SQLCipher snapshot, packages it, recovers the key, extracts the authenticated object, and reopens/verifies the snapshot integrity. Focused backup/package/release tests PASS 23/23; full normal profile PASS 30 files; Core build/typecheck, repository typecheck, architecture, dependency, provenance, secret scan, and `git diff --check` PASS. Repository-wide format check remains affected by pre-existing CRLF/final-newline violations across the Windows checkout and is not treated as evidence for this scoped change. |
| ↳ **3.15** Clean-profile portable restore: full authentication before activation, fresh DB_DEK re-key, REAUTH_REQUIRED reconciliation | **VERIFIED** | 3.14, 3.3 | BKC §12; DS §§31,33; IC §21; VR §21 | 10 CA / 8 scoped | None within 3.15; external custody is satisfied by two matching encrypted removable-media copies held offline with CT private-key originals removed; the verifier reports `EXTERNAL_REQUIRED` because software cannot inspect physical custody | Exact signed private/internal NSIS candidate rebuilt from TUF 1.0.35 metadata was Authenticode-valid under the enrolled thumbprint `23DA4DA3E340B66EC4240B4CC845E4387E5BBDD3` (installer SHA-256 `2AEB7339C4745257F64D3BA4BC87369BFF4D6D233FB3EA87D8C930C3B1CB3860`), installed in isolation with exit code 0, and verified with TUF `modules/*` delegation, authenticated Core transport, controlled stop, wrong-factor rejection, clean-profile restore, fresh DB_DEK commit, recovery marker, and `REAUTH_REQUIRED`; the separately preserved 32-byte recovery secret was not exported and was removed after the drill. Root private keys were verified byte-matching in both encrypted containers before CT deletion; public root metadata remained intact.
| ↳ **3.16** Local restore, wrong-factor/tamper failure atomicity, damaged-state preservation, Recovery Mode entry | **VERIFIED** | 3.12, 3.14, 3.15 | DS §§32,35; SEC §32; BKC §14 | 10 CA / 8 scoped | None within 3.16; physical/offline custody remains externally evidenced by 3.15 and is not inspectable by repository software | Portable and local restore authenticate the complete package before staging, preserve source/destination on wrong-factor and tamper failures, remove rejected staging artifacts, and publish `RECOVERY_REQUIRED` only after exact-marker and secure-storage validation. Focused backup-package tests PASS 6/6; persistence integrity preserves damaged state; lifecycle marker/condition tests PASS; `REPAIR_REQUIRED` is excluded from Core startup; live packaged native IPC local-slot round-trip PASS with mismatched descriptor rejection; full Windows Rust suite PASS 81/81; exact signed installed-candidate drill PASS with both portable and native `WINDOWS_DPAPI_V1` local restore, fresh DB re-key, `REAUTH_REQUIRED`, recovery markers, and descriptor-binding rejection.
| ↳ **3.17** Cross-language backup golden vectors and malicious bounds/KDF/nonce/tag/chunk negative suite | **VERIFIED** | 3.8–3.12, 3.14–3.16; +3.13 if enabled | BKC §14; PLAN §5 Required proof; VR §21 | 10 CA / 8 scoped | None within 3.17; optional passphrase slot remains contract-deferred and is not exposed | Synthetic fixture `tests/fixtures/backup-v1-golden.json` is manifest-bound and independently checked by TypeScript and Rust: RFC/JCS descriptor and chunk AAD bytes, descriptor SHA-256, 4-byte prefix plus big-endian chunk nonce construction, chunk 0/middle/final AES-256-GCM outputs, 128-bit tags, generated-recovery HKDF and BackupDEK unwrap. Focused TypeScript golden/descriptor/chunk/recovery/package tests PASS 16/16; normal unit layer PASS 29 files; full Windows Rust suite PASS 82/82 with 1 separately ignored external-account fixture; existing negative coverage PASS for wrong factors, modified descriptor/slot metadata, nonce/tag/ciphertext corruption, chunk reorder/delete/duplicate/truncate/append, oversized descriptor counts/lengths, and unsafe KDF profiles. |
| ↳ **3.18** Packaged Windows persistence/backup proof with secret-free logs/diagnostics and exact binding evidence | **VERIFIED** | 3.1–3.12, 3.14–3.17; +3.13 if enabled | PLAN §5 Exit; CS §§12–13,27; VR §§20–21,32 | 10 CA / 8 scoped | None within 3.18; section-level 3.CP remains | Exact signed private/internal packaged candidate at source `e3721b9b9d24f3cc41e8f4b7efc0cb9aaee5a73f` passed `qualify-core-runtime.mjs --production-tuf-profile` and `qualify-backup-restore.mjs`: TUF `1.0.35`, target `runtime-manifest.json` SHA-256 `d323b6acaaaa0d508365a0a458485e8cb99b8eb01624c0bb90cc087e2be82609`, authenticated Core transport, protocol major `1`, controlled stop, wrong-factor rejection, clean-profile restore, fresh DB_DEK commit, recovery marker, and `REAUTH_REQUIRED`. Evidence reports `EXTERNAL_INPUT_NOT_RECORDED`, `secretExported=false`, empty stdout/stderr, and contains no recovery-secret path or value; preserved recovery secret length is 32 bytes. Full Windows Rust suite PASS `82/82` with 1 separately ignored external-account fixture, focused golden vectors PASS `16/16`, normal unit layer PASS `29` files, TypeScript typecheck, dependency provenance, and secret scan PASS. This qualifies the exact packaged candidate; it is not Production Complete evidence by itself.
| ↳ **3.CP** SECTION CHECKPOINT — persistence/KDF/JARVIS_BACKUP_V1 portable recovery proof | **VERIFIED** | 3.1–3.12, 3.14–3.18; +3.13 if enabled | PLAN §5 Exit; PLAN §26 checkpoint; RP §4; VR §§14,20–22 | 10 CA / 8 scoped | None within Section 3; repository-wide qualification still contains unrelated future layers with no tests | `tools/checkpoints/phase3-checkpoint.mjs --evidence target/qualification/evidence-3-18-backup.json` PASS; checkpoint unit tests PASS 3/3; all required child rows VERIFIED; packaged evidence reports PASS with TUF shape PASS, wrong-factor rejection, clean-profile restore, fresh DB_DEK commit, recovery marker, `REAUTH_REQUIRED`, redacted recovery-secret input, `secretExported=false`, and no non-empty diagnostics |

### SECTION 4 — Authoritative State / Events

| Section / Subsection | Status | Depends On | Governing Contract / Traceability | Score | Current Gap | Evidence / Result |
|---|---|---|---|---:|---|---|
| **SECTION 4 — Authoritative State / Events** | **VERIFIED** | 3.CP | `Phase 4`; checkpoint: Core State Ready | 10 CA / 8 scoped | None within Section 4; later security and runtime boundaries remain in Sections 5–19 | `4.1`–`4.16` and `4.CP` pass the authoritative state, event, schema, concurrency, configuration, memory, migration, property, crash/reopen, and secret-safety evidence gates. Full normal profile PASS 58 files; Section 4 checkpoint test PASS; no physical intervention or external credential is required for this repository checkpoint. |
| ↳ **4.1** Expand authoritative schema ownership and monotonic migration framework for full Core state | **VERIFIED** | 3.CP, 3.3 | PLAN §6; DS §§6,34; CS §14 | 10 CA / 8 scoped | None within 4.1; later state semantics remain owned by 4.2–4.16 | Schema version 14 adds deterministic migration `0014-approval-lifecycle` after migrations `0002`–`0013`, with SHA-256 ledger identities, full contract-defined logical table ownership, durable approval metadata, monotonic newer-schema rejection, checksum-drift rejection, transactional v1→v14 upgrade, exclusive migration lock with dead-owner-only reclamation, WAL-checkpointed pre-migration encrypted backup preservation, and fail-closed invalid-timestamp/lock behavior. Focused schema tests PASS 11/11; full normal profile PASS 58 files; Core and repository TypeScript typecheck PASS; architecture, dependency, provenance, secret scan, and scoped `git diff --check` PASS. No later subsection policy/state semantics are claimed here. |
| ↳ **4.2** Common durable identifiers, platform/runtime/backend/support identity, and platform-tagged path representation | **VERIFIED** | 4.1, 0.3 | PLAN §6; DS §4; PS §§3,26–27 | 10 CA / 8 scoped | None within 4.2; later mission/state semantics remain owned by 4.3–4.16 | Shared runtime validators strictly validate opaque identity tokens, `PlatformRuntimeIdentity`, `PlatformCompatibility`, and platform-tagged `PlatformPathRef` values with exact-field, enum, length, duplicate, and NUL rejection. Migration `0003-platform-identity-and-path-state` durably stores validated runtime identities, compatibility records, and tagged path references with monotonic versions; invalid writes fail closed and path values are not interpreted by another platform backend. Linux/Android remain modeled only and gain no V1 support. Focused platform tests PASS 3/3; schema/storage tests PASS 8/8; full normal profile PASS 43 files; full TypeScript typecheck, architecture, dependency, provenance, and secret scan PASS. |
| ↳ **4.3** Mission/task/attempt state machines, owning transition policy, durable RESUMING, terminal invariants | **VERIFIED** | 4.1, 0.6 | PLAN §6; DS §§8–11; PS §13; CS §8; VR §8 | 10 CA / 8 scoped | None within 4.3; execution-scope, permission, worker, and later state-owner integrations remain in later subsections | Canonical mission/task/attempt state unions and transition policy are implemented in the protocol runtime. Core persists each transition to its owning table with expected-version CAS, parent ownership checks, causative event in the same transaction, terminal-state rejection, and durable task `RESUMING`; mission resume refuses to bypass paused-task `RESUMING`. Focused state-machine tests PASS 4/4; full normal profile PASS 43 files; full TypeScript typecheck, architecture, dependency, provenance, and secret scan PASS. The local format checker remains blocked by pre-existing repository-wide CRLF/final-newline violations outside this subsection. |
| ↳ **4.4** ExecutionScope persistence and authoritative scope invariants | **VERIFIED** | 4.1, 0.6 | PLAN §6; DS §12; PS §11 | 10 CA / 8 scoped | None within 4.4; authority envelopes, permissions, workers, and later state-owner integrations remain in later subsections | Protocol runtime validates the four canonical scope kinds and independent `DataPolicy` enums. Core stores exactly one versioned execution scope per task with optimistic version checks and a unique database index; `PROJECT_WORKSPACE` requires real project/workspace/environment membership, `INTEGRATION` requires real account/capability bindings, `SYSTEM` validates optional environment context, and `GLOBAL` creates no project/workspace authority. Focused scope/schema tests PASS 11/11; full normal profile PASS 43 files; full TypeScript typecheck, architecture, dependency, provenance, and secret scan PASS. No physical intervention or external credential is required for this subsection. |
| ↳ **4.5** Immutable mission graph/acceptance/dependency persistence structures | **VERIFIED** | 4.3 | PLAN §6; DS §15; PS §14 | 10 CA / 8 scoped | None within 4.5; permission policy, approvals, and later domain-event integration remain in later subsections | Mission-graph protocol validators enforce bounded opaque IDs, unique task/criteria/edge identities, required-output keys, and DAG acyclicity. Core activation validates mission/task ownership, one real execution scope per task, task DataPolicy coherence, authority-envelope containment, and mission-level acceptance policy before atomically inserting the immutable graph version, advancing the mission’s active graph pointer with an expected-version guard, and appending the causative activation event. Migration `0005-mission-graph-immutability` adds database update/delete triggers. Focused graph/schema tests PASS 11/11; full normal profile PASS 43 files; full TypeScript typecheck, architecture, dependency, provenance, and secret scan PASS. No physical intervention or external credential is required for this subsection. |
| ↳ **4.6** Authority-envelope, permission-decision, approval/action-descriptor storage structures without implementing authorization policy | **VERIFIED** | 4.1, 0.6 | PLAN §6; DS §§13–14; PS §§12,17–18 | 10 CA / 8 scoped | None within 4.6; deterministic authorization policy and broader event publication remain in later subsections | Protocol validators preserve typed authority envelopes, permission decisions, canonical action descriptors, and approval statuses; descriptors reject secret-bearing keys and approval digests use JCS → SHA-256 → base64url-no-padding. Core immutably stores authority envelopes, persists permission decisions, binds approval records to descriptor digests, and consumes only approved requests with fresh descriptor equality, optimistic versioning, and single-use transactional protection. Migration `0006-authority-records` adds permission-decision storage and authority-envelope immutability triggers. Focused authority tests PASS 2/2; full normal profile PASS 43 files; full TypeScript typecheck, architecture, dependency, provenance, and secret scan PASS. No physical intervention or external credential is required for this subsection. |
| ↳ **4.7** DataPolicy, exact MoneyAmount, provider quota/usage/budget-reservation persistence primitives | **VERIFIED** | 4.1, 0.6 | PLAN §6; IC §19; DS §§23–24; PS §§4–5,22 | 10 CA / 8 scoped | None within 4.7; provider setup/qualification and broader event integration remain in later subsections | Accounting validators enforce string-only canonical money/quantity values, bounded provider quota sources, cost confidence, reservation states, and no implicit currency arithmetic. Core appends quota/usage facts, persists versioned budget policies, and serializes hard-budget reservations in one transaction using exact integer arithmetic, rejecting stale versions, mixed currencies, and hard-limit overflow. Focused accounting/schema tests PASS 10/10; full normal profile PASS 43 files; full TypeScript typecheck, architecture, dependency, provenance, and secret scan PASS. No physical intervention or external credential is required for this subsection. |
| ↳ **4.8** Worker checkpoints, artifacts, workspace/resource leases, and provider-resume metadata structures | **VERIFIED** | 4.1 | PLAN §6; DS §§16–19; PS §15 | 10 CA / 8 scoped | None within 4.8; provider qualification, event publication, and broader recovery orchestration remain later subsections | Protocol worker records validate bounded checkpoint summaries/evidence, omit private chain-of-thought, preserve optional opaque provider-resume metadata, require artifact identity/hash/policy/retention/storage provenance, and reject SECRET content from the general artifact store. Core binds checkpoints to an existing task/attempt, enforces monotonic per-attempt sequences, stores artifacts immutably, validates producing-attempt/project references, and acquires exclusive workspace/resource leases with durable owner instance/task/attempt metadata and conflict-safe transactions. Focused worker-state tests PASS 3/3; full normal profile PASS 46 files; full TypeScript typecheck, architecture, dependency, provenance, and secret scan PASS. No physical intervention or external credential is required for this subsection. |
| ↳ **4.9** Provider setup/qualification, module, integration, and Proxmox logical state hooks | **VERIFIED** | 4.1 | PLAN §6; DS §§22,25; PS §§19–21 | 10 CA / 8 scoped | None within 4.9; live provider setup probing, TUF module authorization, external integration effects, and Proxmox capability qualification remain later sections | Protocol validators separate provider setup, compatibility, health, capabilities, resource profiles, module execution/integrity/lifecycle, integration status/capabilities, and Proxmox HTTPS/TLS trust/allowlists. Core migration `0007-provider-integration-state` adds versioned integration status JSON; provider profiles/policies, setup and qualification records, module manifests/catalog metadata, integration accounts/capabilities, and Proxmox connections persist with version checks. Setup-ready is rejected across adapter-version change without requalification; credentials remain opaque handles and no public/provider/Proxmox connection is performed. Focused provider-state tests PASS 2/2; full normal profile PASS 46 files; full TypeScript typecheck, architecture, dependency, provenance, and secret scan PASS. No physical intervention or external credential is required for this subsection. |
| ↳ **4.10** Project-policy trust-record/snapshot state hooks | **VERIFIED** | 4.1 | PLAN §6; PPT §§2,4,7; MAN §4.2 | 10 CA / 8 scoped | None within 4.10; live filesystem candidate discovery, authenticated enrollment UI, nested policy resolution, and broader policy authorization integration remain later sections | Protocol validators preserve canonical project/path/scope/hash identity, explicit trust states, acceptance evidence, monotonic revisions, and immutable per-attempt snapshot entries. Core migration `0008-project-policy-trust` adds versioned trust records and immutable one-snapshot-per-attempt storage; trusted records require acceptance evidence, changed content is demoted to `CHANGED_REVIEW_REQUIRED`, only current matching `TRUSTED` revisions enter new snapshots, and snapshot update/delete is blocked by database triggers. Focused project-policy tests PASS 2/2; full normal profile PASS 46 files; full TypeScript typecheck, architecture, dependency, provenance, and secret scan PASS. No physical intervention or external credential is required for this subsection. |
| ↳ **4.11** TUF/update trusted-metadata, release-sequence/security-epoch, backup/update/recovery metadata hooks | **VERIFIED** | 4.1 | PLAN §6; SCT §§10,14–16; DS §6 | 10 CA / 8 scoped | None within 4.11; full live TUF key custody/signing, updater activation, and release qualification remain later sections | Protocol validators preserve TUF 1.0.35 role metadata versions/hashes, trusted-root and security-epoch floors, release sequence/rollback metadata, incident reason codes, and backup/update/recovery operation states without private keys. Core migration `0009-update-trust-state` durably stores crash-safe trusted metadata, release floors/revocation, incidents, and operation lifecycle records; exact BigInt comparisons reject metadata and release rollback, committed operations cannot regress. Focused update-trust tests PASS 2/2; full normal profile PASS 46 files; full TypeScript typecheck, architecture, dependency, provenance, and secret scan PASS. Live signing/key-custody and final update qualification are intentionally not claimed here and require no physical intervention for this state-hook subsection. |
| ↳ **4.12** Versioned append-oriented domain-event model, correlation/causation, post-commit publication, dedup foundation | **VERIFIED** | 4.1, 3.3 | PLAN §6; DS §§7,18; PS §23 | 10 CA / 8 scoped | None within 4.12; atomic state-transition integration remains in 4.13 | Protocol domain-event envelopes validate UUIDv7 identity, UTC timestamps, stable dot-notation types, payload versions, actor types, correlation/causation, bounded non-secret payloads, and external provenance. Migration `0010-domain-event-provenance` durably stores complete envelopes and source identity; append allocates monotonic aggregate versions transactionally, records provenance/deduplication, returns the existing event for replayed keys, and notifies listeners only after commit while isolating consumer failures. Focused domain-event/schema tests PASS 11/11; full normal profile PASS 47 files; full TypeScript typecheck, architecture, dependency, provenance, secret scan, and safety checks PASS. No physical intervention or external credential is required for this subsection. |
| ↳ **4.13** Optimistic concurrency and repository transaction pattern: state + causative event/audit + invariant rows atomically | **VERIFIED** | 4.3–4.12 | PLAN §6; DS §§7–8; CS §§12–13 | 10 CA / 8 scoped | None within 4.13; configuration activation and broader state coverage remain in 4.14–4.16 | Core state-changing repository transitions assert expected versions and legal state/parent invariants, update authoritative rows, append causative events, and append minimal audit evidence through one SQLite transaction; event/audit insertion failure rolls back the state mutation. Focused schema/state tests PASS 13/13, including duplicate-event rollback proving state, event, and audit atomicity; full normal profile PASS 47 files; full TypeScript typecheck, architecture, dependency, provenance, secret scan, and safety checks PASS. No physical intervention or external credential is required for this subsection. |
| ↳ **4.14** Authoritative non-secret configuration service with validated candidate staging and atomic activation | **VERIFIED** | 4.1, 4.13 | OPS §11; PS §24; CS §30 | 10 CA / 8 scoped | None within 4.14; conversation/memory separation and later state coverage remain in 4.15–4.16 | Protocol configuration candidates validate canonical domains, positive schema versions, bounded JSON values, no secret-bearing keys, and no unknown feature/flag activation keys. Migration `0011-configuration-authority` adds Core-owned staged candidates and one versioned active configuration per domain; activation checks the expected version, revalidates stored candidates, updates the active pointer, marks the candidate, and writes an audit record in one transaction. Focused configuration/schema tests PASS 12/12; full normal profile PASS 48 files; full TypeScript typecheck, architecture, dependency, provenance, secret scan, and safety checks PASS. No physical intervention or external credential is required for this subsection. |
| ↳ **4.15** Conversation/memory storage separation, confidence/revision metadata, and retention anchors | **VERIFIED** | 4.1 | DS §20; OPS §13 | 10 CA / 8 scoped | None within 4.15; final state/crash test consolidation remains in 4.16 | Protocol memory records preserve user/global, project, mission, task, and session scopes, VERIFIED/CONFIRMED/INFERRED/STALE confidence, source identity/revision, observation/verification/staleness timestamps, DataPolicy, and retention-anchor identity while rejecting SECRET content. Conversation records/messages use separate storage from memories, enforce common DataPolicy, and retain explicit retention anchors; migration `0012-memory-retention` adds versioned anchors with non-shortening updates and memory revisions remain append-oriented. Focused memory/schema tests PASS 12/12; full normal profile PASS 48 files; full TypeScript typecheck, architecture, dependency, provenance, secret scan, and safety checks PASS. No physical intervention or external credential is required for this subsection. |
| ↳ **4.16** State/property/migration/crash tests proving illegal transitions, stale-write rejection, no raw secrets/native handles | **VERIFIED** | 4.2–4.15 | PLAN §6 Exit; VR §8; DS §37; CS §§8,14 | 10 CA / 8 scoped | None within 4.16 | Dedicated Section 4 evidence test proves idempotent schema migration ledger, illegal mission transition rejection, stale version rejection, state/event/audit rollback on duplicate event, and absence of raw secret material in authoritative column names. Existing property-state-machine, encrypted WAL crash/reopen, integrity, migration-lock, and full normal safety/architecture suites also pass. Focused Section 4 evidence tests PASS 1/1; full normal profile PASS 49 files before checkpoint test. No physical intervention or external credential is required. |
| ↳ **4.CP** SECTION CHECKPOINT — authoritative Core state, events, schema and concurrency invariants | **VERIFIED** | 4.1–4.16 | PLAN §6 Exit; PLAN §26 checkpoint; VR §§7–10 | 10 CA / 8 scoped | None within Section 4 | Section 4 child rows 4.1–4.16 are VERIFIED; live matrix checkpoint test PASS; full normal profile PASS 58 files including migration, approval lifecycle, state-machine, event/audit atomicity, configuration, memory/retention, property, crash/reopen, architecture, dependency, provenance, secret-scan, and safety evidence. Section 5 may begin. No physical intervention or external credential is required for this repository checkpoint. |

### SECTION 5 — Session Security / PermissionEngine / Approval

| Section / Subsection | Status | Depends On | Governing Contract / Traceability | Score | Current Gap | Evidence / Result |
|---|---|---|---|---:|---|---|
| **SECTION 5 — Session Security / PermissionEngine / Approval** | **VERIFIED** | 4.CP | `Phase 5`; checkpoint: Security/Permission Boundary Ready | 10 CA / 8 scoped | No repository-side gap remains within Section 5; later provider/tool execution and integration wiring remain downstream | Section 4 checkpoint is VERIFIED; 5.1–5.14 and 5.CP are VERIFIED. The Section 5 checkpoint verifies session trust, deterministic permission precedence, canonical approval binding, fresh target resolution, destructive confirmation, reason-coded audit evidence, and the explicit same-user threat limit. No physical intervention or external credential is required for this repository section. |
| ↳ **5.1** Session password creation/unlock/lock state, progressive cooldown, and production KDF verifier | **VERIFIED** | 4.CP, 3.5 | PLAN §7; IC §9; SEC §§4–5; VR §14 | 10 CA / 8 scoped | None within 5.1; versioned rehash/recovery is owned by 5.2 and native Windows lock/idle suppression by 5.3 | Protocol session-security validation enforces locked/unlocked invariants and bounded progressive cooldown; Core migration `0013-session-security` adds durable session state; initialization persists only the versioned Argon2id verifier metadata, authentication failures and cooldown use transactional state+event+audit writes, correct verification unlocks and resets failures, explicit lock returns to `LOCKED`, and duplicate evidence rolls back the state change. The Windows native boundary now derives and verifies Argon2id `0x13` using the production floor (65536 KiB, 3 passes, 4 lanes, 16-byte salt, 32-byte output) with bounded profile validation and constant-time comparison. Focused session tests PASS 3/3; full normal profile PASS 51 files; strict TypeScript PASS; Windows Rust suite PASS 82 passed / 1 separately logged-on-account test ignored. No physical intervention or external credential is required for 5.1. |
| ↳ **5.2** Versioned KDF rehash/upgrade and explicit recovery-factor password-reset/recovery workflow | **VERIFIED** | 5.1, 3.15 | PLAN §7; SEC §4; DS §33; VR §14 | 10 CA / 8 scoped | No repository-side gap remains for the verifier upgrade/reset boundary; production signed disaster-drill evidence remains an external release qualification concern owned by 3.15 and is not claimed here | Core stores exact approved verifier metadata and exposes compare-and-set versioned upgrades only after an unlocked session has supplied current-verifier verification evidence; explicit password changes require the exact change event and confirmation. Recovery reset requires a locked expected session version, explicit confirmation, and generated-recovery verification evidence containing only non-secret backup/slot/digest metadata; it clears cooldown/failure state and records verifier/state event+audit atomically. Focused session-security tests PASS 4/4, focused backup-package tests PASS 15/15, full normal profile PASS 51 files, strict TypeScript PASS, and scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **5.3** PlatformSessionObserver lock/sign-out/idle integration and locked UI/voice/notification data suppression | **VERIFIED** | 5.1, 1.9 | PLAN §7; SEC §5; PP §17; VR §14 | 10 CA / 8 scoped | No repository-side gap remains for the observer, authoritative lock-reason mapping, or locked-session content suppression boundary; live packaged Windows UI/voice qualification remains a later release gate | Existing Windows `PlatformSessionObserver` now carries explicit `OS_SESSION_LOCK`, `OS_SESSION_END`, and configured `IDLE` reasons, queries bounded `GetLastInputInfo` idle state, and fails closed on probe failure. `WindowsNativeBroker` exposes the configured idle observer; Core `lockSessionForPlatform` clears the active session and persists the native reason transactionally with event/audit evidence; protocol notification policy silences and defers non-public content while locked while preserving bounded public-safe notices. Focused session/privacy tests PASS 7/7, full normal profile PASS 52 files, strict TypeScript PASS, Windows Rust suite PASS 84 passed / 1 separately logged-on-account test ignored, and scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **5.4** Credential Broker over PlatformSecureStorage with context-scoped opaque handles, rotate/revoke/delete lifecycle, and secret-exclusion guarantees | **VERIFIED** | 3.4, 4.CP | PLAN §7; SEC §6; CS §17 | 10 CA / 8 scoped | No repository-side gap remains for the narrow Windows broker surface; provider-specific credential capability integration remains downstream | Windows secure storage now exposes typed `CredentialContext`, opaque DPAPI handles, context-bound put/get/rotate/delete operations, no enumeration API, and transient `CredentialSecret` zeroization. Wrong account/capability context cannot recover the secret; rotation publishes a new handle before removing the old one and cleans up on failure. Core-facing native broker wrappers expose only the typed surface and preserve the existing DB_DEK/BackupDEK role separation. Focused/native secure-storage tests PASS, Windows Rust suite PASS 85 passed / 1 separately logged-on-account test ignored, strict TypeScript PASS, and scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **5.5** DataSensitivity/DataLocality enforcement and deterministic audited declassification boundary | **VERIFIED** | 4.7 | IC §19; SEC §8; PS §4 | 10 CA / 8 scoped | No repository-side gap remains for strictest-policy inheritance and the authenticated declassification boundary; full PermissionEngine/approval integration remains downstream | Protocol `strictestDataPolicy` preserves the strictest sensitivity and `LOCAL_ONLY` locality across derived inputs. Core `declassifyDataPolicy` requires an unlocked session, canonical event type, explicit confirmation, a genuine policy reduction, bounded non-secret reason text, and atomic event/audit persistence; duplicate decisions and locality-tightening attempts fail closed. Focused data-policy tests PASS 4/4, full normal profile PASS 53 files, strict TypeScript PASS, and scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **5.6** Content-authority/prompt-injection source labeling and structured-AI-output validation foundation | **VERIFIED** | 5.5, 0.6 | SEC §§9–12; VR §16 | 10 CA / 8 scoped | No repository-side gap remains for the bounded provenance label and fail-closed structured-AI-output foundation; provider-specific adapters and PermissionEngine integration remain downstream | Protocol labels distinguish system/user/project/verified/memory/external/artifact/AI sources; scoped instruction labels require Core resolution evidence, while external/task/AI content is content-only and cannot self-elevate. Structured AI output is versioned, bounded, source-bound to AI_OUTPUT, rejects unknown fields, excessive depth/size, secret-bearing or authority-bearing data keys, invalid identity/timestamps, and malformed items. Focused content-authority tests PASS 3/3; full normal profile PASS 54 files; strict TypeScript PASS; scoped `git diff --check` PASS. Repository-wide format check remains blocked by pre-existing unrelated CRLF/final-newline findings. No physical intervention or external credential is required for this repository subsection. |
| ↳ **5.7** AuthorityEnvelopeService immutable action/scope/system/data/budget containment semantics | **VERIFIED** | 4.6, 4.4 | PLAN §7; IC §13; DS §13; PS §12 | 10 CA / 8 scoped | No repository-side gap remains for the immutable envelope containment foundation; full PermissionEngine precedence and live tool/target integration remain downstream | Authority envelope validation now normalizes immutable typed scopes, DataPolicy, and non-negative exact MoneyAmount budget caps. `assertAuthorityEnvelopeContains` fails closed unless the requested action is allowed and not denied, the execution scope is equal or narrower, external-system IDs are contained, DataPolicy does not exceed sensitivity/locality, and estimated budget remains within the exact same-currency cap. Integration capability subsets and project/environment identities are checked without display-name widening; absent budget caps cannot invent an estimated spend ceiling. Focused containment/authority tests PASS 5/5, full normal profile PASS 55 files, strict TypeScript PASS, and scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **5.8** PermissionEngine exact deterministic precedence, risk classes, standing permissions, and precedent limits | **VERIFIED** | 5.7, 5.1 | PLAN §7; IC §13; SEC §§13–15; CS §9; VR §15 | 10 CA / 8 scoped | No repository-side gap remains for the pure deterministic precedence/risk foundation; persisted policy records and live capability/precondition facts remain downstream | Policy `evaluatePermission` applies mandatory invariant, explicit deny, session, envelope, capability/identity, and preflight gates in exact order. It requires current or exact non-expired policy-permitted standing authority, never uses precedent to authorize HIGH/CRITICAL actions, returns `REQUIRE_APPROVAL` for insufficient higher-risk authority, and always requires fresh final confirmation for CRITICAL/materially unrecoverable work. Focused PermissionEngine tests PASS 3/3, full normal profile PASS 56 files, strict TypeScript PASS, and scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **5.9** Pre-ALLOW gates for platform/setup/integrity/project-policy/supply-chain/locality/budget/resource/precondition facts | **VERIFIED** | 5.8, 0.4 | IC §13 step 6; SEC §13; PLAN §7 | 10 CA / 8 scoped | No repository-side gap remains for the typed pre-ALLOW fact aggregation foundation; live provider/project-policy/supply-chain adapters remain downstream | Policy `evaluatePreAllowGates` validates nine named facts in deterministic order: platform capability, provider setup, integrity, project-policy trust, supply-chain trust, locality, budget, resource, and precondition. Applicable facts must be `PASS`; only explicit `NOT_APPLICABLE` passes without evidence; `FAIL` and `UNKNOWN` block with the gate identity/reason. Focused pre-ALLOW tests PASS 3/3, full normal profile PASS 57 files, strict TypeScript PASS, and scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **5.10** CanonicalActionDescriptorV1 single shared builder, RFC8785/SHA-256/base64url Rust+TS golden vectors | **VERIFIED** | 4.6, 0.6 | PLAN §7; IC §15; PS §§18,25; SEC §17; CS §18; VR §10 | 10 CA / 8 scoped | No repository-side gap remains for the shared descriptor identity/canonicalization/digest foundation; approval lifecycle and fresh target re-resolution remain downstream | Protocol owns the V1 descriptor builder and bounded RFC 8785-compatible canonicalization, rejecting identity injection, cycles, non-JSON values, unsafe integers, negative zero, and invalid Unicode. Core approval hashing now consumes the shared canonicalizer and emits exact SHA-256/base64url-no-padding digests. The golden fixture is verified by TypeScript and an independent native Rust test. Focused authority/canonical tests PASS 4/4, Rust vector PASS 1/1, full normal profile PASS 58 files, strict TypeScript PASS, and scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **5.11** ApprovalService issue/decision/expiry/cancel/single-use transactional consumption and replay protection | **VERIFIED** | 5.8, 5.10, 4.13 | PLAN §7; IC §15; DS §14; VR §§8,15 | 10 CA / 8 scoped | No repository-side gap remains for approval issue/decision/expiry/cancel/consumption; fresh material target re-resolution and destructive final confirmation remain owned by 5.12 | Migration `0014-approval-lifecycle` persists bounded approval metadata needed for expiry and decision validation. Protocol validates bounded `ApprovalDecision` records; Core `decideApproval`, `cancelApproval`, and `expireApproval` use pending-state/version CAS and transactional decision/state evidence, while `consumeApproval` revalidates expiry and the exact current descriptor digest before one-time `APPROVED`→`CONSUMED` transition. Expired, cancelled, stale, duplicate, and replayed operations fail closed. Focused core schema/authority tests PASS 11/11; approval lifecycle tests PASS 3/3; full normal profile PASS 58 files; strict TypeScript PASS; scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **5.12** Fresh material target re-resolution and mandatory destructive final confirmation immediately before execution | **VERIFIED** | 5.11 | IC §§14–15; SEC §§16–18; OPS §23 | 10 CA / 8 scoped | No repository-side gap remains for fresh target evidence and the destructive final-confirmation boundary; provider-native conditional mutation remains owned by later integration subsections | Approval consumption now requires bounded fresh target-resolution evidence bound to the recomputed descriptor digest and rejects future/stale evidence. Destructive/materially unrecoverable approvals require a fresh confirmation within the bounded evidence window, bound to approval ID, descriptor digest, authenticated session, and exact action/target/environment/consequence summaries; missing, changed, stale, future, or non-destructive misuse fails closed. Focused authority lifecycle/canonical tests PASS 10/10; full normal profile PASS 58 files; strict TypeScript PASS; scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **5.13** Session/approval/permission audit reason codes, adversarial scenarios, and same-user threat-limit wording | **VERIFIED** | 5.1–5.12 | PLAN §7 Exit; SEC §§31–34; VR §§14–16 | 10 CA / 8 scoped | No repository-side gap remains for typed reason-coded security audit evidence and explicit same-user threat limits; broader audit coverage for later integrations remains owned by those subsections | Protocol defines bounded secret-free security audit event/reason-code unions and explicit same-user threat-limit wording without overclaiming malware, Administrator/kernel, or physical-session isolation. Core writes permission and approval issue/decision/expiry/cancellation/consumption audit evidence transactionally with the related state; adversarial tests cover secret-bearing audit rejection, unsupported fields, deterministic authorization precedence, precedent limits, critical confirmation, and approval target/session mismatch. Focused audit/authority/permission tests PASS 9/9; full normal profile PASS 59 files; strict TypeScript PASS; scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **5.14** Mission Control session and approval UX: exact action/target/environment/consequence, keyboard path, no color-only authority | **VERIFIED** | 5.3, 5.11 | UI §§14,20–21; OPS §23; VR §11 | 10 CA / 8 scoped | No repository-side gap remains for the bounded approval-review presentation; authenticated Core wiring and end-to-end packaged Windows UX remain downstream integration/release gates | Mission Control now exposes a reusable approval-review surface with explicit text labels for action, exact target, environment, expected consequence, rollback/backup when available, expiry, approval status, material-change invalidation, and keyboard-accessible approve/reject controls. Destructive approval uses a text-labelled `Confirm and continue` path and status is never conveyed by color alone; untrusted summaries remain inert text. Focused desktop workspace tests PASS 11/11; full normal profile PASS 59 files; strict TypeScript PASS; scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **5.CP** SECTION CHECKPOINT — session trust, deterministic authorization, canonical approvals, and destructive boundary | **VERIFIED** | 5.1–5.14 | PLAN §7 Exit; PLAN §26 checkpoint; VR §§14–16 | 10 CA / 8 scoped | No repository-side gap remains within Section 5; later provider/tool execution and integration wiring remain downstream | All Section 5 child rows 5.1–5.14 are VERIFIED; the Section 5 checkpoint test verifies the parent/child matrix state and the integrated invariant that CRITICAL/materially unrecoverable actions require fresh final confirmation before ALLOW. Focused Section 5 checkpoint PASS; full normal profile PASS 60 files; strict TypeScript PASS; scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository checkpoint. |

### SECTION 6 — Projects / Scopes / Context / Memory / Project-Policy Trust

| Section / Subsection | Status | Depends On | Governing Contract / Traceability | Score | Current Gap | Evidence / Result |
|---|---|---|---|---:|---|---|
| **SECTION 6 — Projects / Scopes / Context / Memory / Project-Policy Trust** | **VERIFIED** | 5.CP | `Phase 6`; checkpoint: Project Policy Trust Boundary Ready | 10 CA / 8 scoped | None within the Section 6 repository checkpoint; connected Windows end-to-end and later release qualification remain cumulative gates | 6.1–6.15 and the Section 6 integration checkpoint are VERIFIED. The checkpoint confirms exact project/workspace/integration/system/global scope boundaries, canonical Windows path identity, nested policy trust/applicability, immutable snapshot revalidation, source-labeled bounded context, scoped/ranked memory with live-state dominance, conversation separation/LOCAL_ONLY inheritance, and read-only diagnostics. Section 6 checkpoint tests PASS 1/1; all normal-eligible layers pass individually; strict TypeScript and scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository checkpoint. |
| ↳ **6.1** ProjectRegistry aliases/environments/workspaces/worktrees and canonical project identity | **VERIFIED** | 5.CP, 4.1 | PLAN §8; RP §6; DS §6 | 10 CA / 8 scoped | No repository-side gap remains for typed registry persistence and immutable canonical project/workspace identity; full Windows path canonicalization remains owned by 6.2 | Protocol defines bounded project, alias, environment, primary-workspace, and worktree records with platform-tagged paths and no secret-bearing extension fields. Core persists them transactionally, requires referenced projects, rejects alias/environment/workspace cross-project moves, prevents duplicate canonical identities/paths, and permits only bounded branch/source updates after identity checks. Focused project-registry tests PASS 2/2; full normal profile PASS 61 files; strict TypeScript PASS; scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **6.2** Full Windows canonical path/security backend: traversal, reparse/junction/symlink, UNC/drive/root/case identity | **VERIFIED** | 6.1, 1.10 | PLAN §8; PP §13; SEC §16; CS §16 | 10 CA / 8 scoped | No repository-side gap remains for the current Windows path-identity foundation; deeper file-target TOCTOU and project-policy use remain downstream | Windows native path identity now inspects the caller path before and after canonicalization to reject reparse/junction/symlink escapes, rejects traversal, unsupported UNC/device/alternate roots, unintended drive-root targets, and non-directory project roots, and preserves a case-insensitive canonical identity key. Native Rust path tests PASS 5/5; full normal profile PASS 61 files; strict TypeScript PASS; scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **6.3** Execution-scope resolution/membership: PROJECT_WORKSPACE vs INTEGRATION/SYSTEM/GLOBAL with no fake filesystem authority | **VERIFIED** | 6.1, 6.2, 4.4 | PLAN §8; IC §10; DS §12; PS §11 | 10 CA / 8 scoped | No repository-side gap remains for typed scope discrimination and membership enforcement; consequential tool execution and later scope revalidation remain downstream | Existing Core scope persistence validates exactly one typed scope per task, requires project/workspace/environment membership for `PROJECT_WORKSPACE`, validates integration account/capability bindings, permits explicit system capabilities, and keeps `GLOBAL` free of implicit filesystem authority. The negative suite now covers cross-project environment injection and confirms global scope creates no project rows. Focused execution-scope tests PASS 3/3; strict TypeScript PASS; scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **6.4** Project registration/open workflow, AGENTS.md candidate discovery without automatic trust, and PROJECT_POLICY_DECISION_REQUIRED before consequential mutation when undecided | **VERIFIED** | 6.1, 6.2 | PPT §§3,5; RP §6 | 10 CA / 8 scoped | No repository-side gap remains for explicit untrusted candidate registration and the consequential mutation gate; authenticated UI review and actual filesystem discovery integration remain downstream | Only `AGENTS.md` is accepted as a typed project-policy candidate. Core registration is idempotent and begins at `UNTRUSTED_CANDIDATE`; no candidate is silently trusted. Consequential mutation fails with typed `PROJECT_POLICY_DECISION_REQUIRED` until the record is explicitly trusted or disabled, while non-consequential work remains allowed under untrusted-content labeling. Focused project-policy tests PASS 2/2; full normal profile PASS 61 files; strict TypeScript PASS; scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **6.5** Canonical project-policy identity: project/path/scope/content hash + Git provenance | **VERIFIED** | 6.4 | PPT §§3–4 | 10 CA / 8 scoped | No repository-side gap remains for bounded canonical policy identity and provenance persistence; Windows path binding and nested-policy applicability remain downstream | `ProjectPolicyTrustRecord` binds the registered `projectId`, traversal-free canonical `AGENTS.md` relative path, bounded scope root, lowercase SHA-256 content hash, and optional bounded Git blob/commit provenance. Core preserves this identity across revisions and rejects project/path/scope moves; changed trusted content becomes `CHANGED_REVIEW_REQUIRED` instead of silently transferring trust. Focused project-policy tests PASS 2/2; strict TypeScript PASS; scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **6.6** Authenticated project-policy review/enroll/disable/revoke workflow and durable trust records | **VERIFIED** | 6.5, 5.1, 4.10 | PLAN §8; PPT §§4–6,13 | 10 CA / 8 scoped | No repository-side gap remains for session-bound trust decisions, revision CAS, and durable audit evidence; authenticated UI review/presentation remains downstream | Core provides session-bound `TRUST`, `DISABLE`, and `REVOKE` decisions, requiring an authoritative unlocked user/session and expected revision. Trust enrollment records acceptance session/time; stale or locked-session decisions fail closed; each decision atomically updates the trust record and appends bounded event/audit evidence. Focused project-policy tests PASS 2/2; full normal profile PASS 61 files; strict TypeScript PASS; scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **6.7** Policy hash/path/project/branch/worktree change detection → CHANGED_REVIEW_REQUIRED | **VERIFIED** | 6.6 | PPT §§7–8,11 | 10 CA / 8 scoped | No repository-side gap remains for content, path/scope, and Git provenance drift detection; live branch/worktree observation and file-level TOCTOU revalidation remain downstream | Trusted policy updates with changed content hash, canonical path/scope, Git blob, or source-commit provenance are demoted to `CHANGED_REVIEW_REQUIRED`; cross-project identity moves fail closed, and the changed record cannot enter a new immutable snapshot until re-enrollment. Focused project-policy tests PASS 2/2; full normal profile PASS 61 files; strict TypeScript PASS; scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **6.8** Nested policy separate enrollment, subtree applicability, precedence, and conflict-safe blocking | **VERIFIED** | 6.6, 6.7 | PPT §10 | 10 CA / 8 scoped | No repository-side gap remains for separate nested enrollment, applicable-subtree ordering, or same-scope ambiguity blocking; file discovery and live TOCTOU revalidation remain downstream | Core resolves only trusted policies whose canonical scope contains the target, orders applicable policies from parent to nested scope, ignores untrusted/changed/disabled/revoked nested candidates, and fails closed on multiple trusted policies at the same scope. Focused project-policy tests PASS 2/2; full normal profile PASS 61 files; strict TypeScript PASS; scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **6.9** Contextually HIGH trusted-policy mutation; worker/file write cannot auto-trust resulting content | **VERIFIED** | 6.6, 5.8, 5.12 | PPT §9; SEC §10 | 10 CA / 8 scoped | No repository-side gap remains for the HIGH-risk, target-bound policy mutation admission guard; the concrete filesystem write integration and post-write re-enrollment UI remain downstream | Trusted project-policy mutation admission requires HIGH risk, validates the exact enrolled `AGENTS.md` identity plus proposed content hash, permits only an authenticated-user actor, delegates risk/approval precedence to PermissionEngine, and denies worker/tool self-authorization. Existing persistence demotes changed trusted content to `CHANGED_REVIEW_REQUIRED`, so new content is not auto-trusted. Focused project-policy tests PASS 3/3; full normal profile PASS 61 files; strict TypeScript PASS; scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **6.10** Immutable applicable policy snapshots per attempt plus new-attempt/RESUMING/consequential revalidation hooks | **VERIFIED** | 6.6–6.9, 4.8 | PPT §§7,13; PLAN §8 | 10 CA / 8 scoped | No repository-side gap remains for immutable per-attempt snapshots and database trust-identity revalidation; live file-content observation and task lifecycle wiring remain downstream | Core persists one immutable snapshot per attempt, requires current trusted revision/content identity at creation, and revalidates that identity for `NEW_ATTEMPT`, `RESUMING`, or `CONSEQUENTIAL_ACTION`, failing closed after trust-state or revision/hash drift. Focused project-policy tests PASS 3/3; full normal profile PASS 61 files; strict TypeScript PASS; scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **6.11** ContextManager authority/source labeling, minimal context packaging, and untrusted-repository separation | **VERIFIED** | 5.6, 6.6 | PLAN §8; SEC §11; PPT §12 | 10 CA / 8 scoped | No repository-side gap remains for bounded source-labeled context packaging; full Core task/attempt context lifecycle wiring remains downstream | Context authority labels preserve Core resolution evidence, reject AI/external/task-artifact self-authority, and classify trusted project policy as scoped instruction only when resolution evidence is present. Bounded context packages enforce unique item IDs, bounded item/content totals, exact trusted-policy revision evidence, and content-only handling for untrusted repository material. Focused content-authority tests PASS 4/4; full normal profile PASS 61 files; strict TypeScript PASS; scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **6.12** MemoryService scope/confidence/revisions/DataPolicy/ranked retrieval/live-state dominance | **VERIFIED** | 4.15, 6.11 | PLAN §8; DS §20; OPS §13 | 10 CA / 8 scoped | No repository-side gap remains for bounded scoped/ranked memory retrieval; full task-context orchestration and richer live-state adapters remain downstream | Core validates bounded retrieval scopes/query terms/limits/DataPolicy, ranks by applicable scope, confidence, query relevance, and freshness, preserves source/revision metadata, and excludes memory whose source is identified as authoritative live state. Focused memory tests PASS 3/3; normal eligible layers PASS individually (unit, property-state-machine, schema-cross-language-contract, platform-architecture-contracts, safety-adversarial); strict TypeScript and scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **6.13** Conversation/history separation, retention semantics, and LOCAL_ONLY inheritance | **VERIFIED** | 6.12, 4.15 | DS §20; SEC §8 | 10 CA / 8 scoped | No repository-side gap remains for separate durable conversation/memory stores, retention-anchor identity/extension, or conversation DataPolicy/LOCAL_ONLY inheritance | Core persists conversations, messages, memories, revisions, and retention anchors in separate authoritative tables; anchor identity cannot change or shorten; message DataPolicy must exactly match its parent conversation, preventing LOCAL_ONLY inheritance weakening. Focused memory/conversation tests PASS 3/3; all normal-eligible layers pass individually; strict TypeScript and scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **6.14** Project/policy/context/memory UI and authoritative policy-path/hash/revision diagnostics | **VERIFIED** | 6.6–6.13, 1.13 | PPT §14; UI §§8,15,25 | 10 CA / 8 scoped | No repository-side gap remains for read-only policy diagnostics; live Core transport binding and full connected Windows UX qualification remain release-gate work | Mission Control exposes a read-only project-policy diagnostics model/panel with truthful status and exact canonical path, scope root, content SHA-256, trust revision, Git blob, and source commit fields, rendered through inert text and accessible status semantics. Desktop workspace tests PASS 11/11; desktop strict TypeScript PASS; all normal-eligible layers pass individually; scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **6.15** Project/path/policy/memory adversarial and conformance suite | **VERIFIED** | 6.2–6.14 | PLAN §8 Exit; PPT §15; VR §§8,16,24 | 10 CA / 8 scoped | No repository-side gap remains in the bounded project/path/policy/context/memory adversarial suite; live Windows file observation and connected end-to-end qualification remain release-gate work | Conformance suite covers traversal rejection, same-scope trusted-policy conflict blocking, worker mutation denial, untrusted context authority spoofing, bounded retrieval scope, and SECRET/locality memory rejection. Focused conformance tests PASS 2/2; all normal-eligible layers pass individually; strict TypeScript and scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **6.CP** SECTION CHECKPOINT — exact scopes, canonical paths, memory/context, and project-policy trust boundary | **VERIFIED** | 6.1–6.15 | PLAN §8 Exit; PLAN §26 checkpoint | 10 CA / 8 scoped | No repository-side checkpoint gap remains; connected Windows end-to-end qualification remains a later release gate | Integrated checkpoint asserts all 6.1–6.15 rows are VERIFIED and exercises parent/nested trusted-policy resolution, scoped trusted context packaging, and bounded local memory retrieval with authoritative live-state exclusions. Checkpoint test PASS 1/1; all normal-eligible layers pass individually; strict TypeScript and scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository checkpoint. |

### SECTION 7 — Codex Windows Provider Setup / Sandbox Qualification

| Section / Subsection | Status | Depends On | Governing Contract / Traceability | Score | Current Gap | Evidence / Result |
|---|---|---|---|---:|---|---|
| **SECTION 7 — Codex Windows Provider Setup / Sandbox Qualification** | **IN PROGRESS** | 6.CP | `Phase 7`; checkpoint: Codex Windows Setup/Provider/Sandbox Ready | — | 7.5 remains in progress: JARVIS-owned CLI distribution registration and qualified UAC lifecycle are incomplete; 7.6–7.17 remain | 7.1–7.4 are VERIFIED. The Codex CLI installation and provider-managed sandbox are live-qualified, while the repository mediator still fails closed before JARVIS-owned UAC launch. |
| ↳ **7.1** Provider Registry/Router/Supervisor/SetupCoordinator common states plus lifecycle/capability interfaces supporting qualified one-shot, warm, persistent, streaming, resumable and local-server adapters | **VERIFIED** | 6.CP, 4.9 | PLAN §9; RT §§14–16; PS §19; CS §20 | 10 CA / 8 scoped | No repository-side gap remains for typed provider adapter modes/lifecycle/capability contracts; Codex distribution identity, setup workflow, sandbox qualification, and live provider execution remain downstream | Provider protocol now separates existing setup/compatibility/health states from a bounded adapter contract covering one-shot, warm, persistent, streaming, resumable, and local-server modes; required lifecycle states; capability-to-mode/locality binding; cancellation/resumption flags; and bounded startup/execution timeouts. Focused provider tests PASS 4/4; unit layer PASS; strict TypeScript and scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **7.2** Codex Windows distribution/executable/helper identity discovery and release compatibility policy | **VERIFIED** | 7.1 | PLAN §9; RP §5; SEC §23 | 10 CA / 8 scoped | Live Windows Codex discovery, setup, sandbox, and provider conformance evidence remain downstream; protocol compatibility does not claim a real installed distribution is qualified | Provider protocol now validates a platform-bound distribution identity containing exact distribution/interface/adapter/version metadata, canonical executable and optional setup-helper identity, file versions, and optional SHA-256 evidence. The release policy evaluator fail-closes on provider/adapter/platform/distribution/interface/executable/helper/version mismatches, supports exact and bounded semver ranges, and returns `VERSION_UNKNOWN` when identity/policy evidence is incomplete. Focused provider tests PASS 4/4; normal unit, property-state-machine, schema-cross-language-contract, platform-architecture-contracts, and safety-adversarial layers PASS; strict TypeScript and scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **7.3** Stable structured/non-interactive Codex adapter, bounded output validation, normalized errors/events | **VERIFIED** | 7.2, 5.6 | PLAN §9; IC §17; RP §5; CS §20 | 10 CA / 8 scoped | Actual Codex process launch, platform containment, and live provider conformance remain downstream; protocol validation does not claim an installed provider is ready | Provider protocol now defines stable versioned adapter results/events, validates structured AI output through the existing content-authority boundary, enforces output-or-normalized-error exclusivity, bounds safe error messages, rejects control characters/raw provider details, and requires failed-event error semantics. Focused provider tests PASS 5/5; normal unit, property-state-machine, schema-cross-language-contract, platform-architecture-contracts, and safety-adversarial layers PASS; strict TypeScript and scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **7.4** Provider setup/repair state machine and explicit user-visible SetupCoordinator workflow | **VERIFIED** | 7.2, 4.9 | PLAN §9; RT §15; DS §22 | 10 CA / 8 scoped | Actual authenticated Core command, UAC invocation, and setup-helper execution remain downstream; this protocol boundary intentionally does not authorize elevation | Provider protocol now models deterministic setup actions and transitions: explicit authenticated-user start, helper-exit-without-readiness, independent probe success/failure, cancellation, provider-update invalidation, repair/setup-required presentation, and honest user-action/readiness flags. Focused provider tests PASS 6/6; normal unit, property-state-machine, schema-cross-language-contract, platform-architecture-contracts, and safety-adversarial layers PASS; strict TypeScript and scoped `git diff --check` PASS. No physical intervention or external credential is required for this repository subsection. |
| ↳ **7.5** Qualified setup-helper identity validation and bounded UAC through PlatformPrivilegeMediator | **IN PROGRESS** | 7.4, 2.6 | PLAN §9; IC §12; SEC §22; VR §17 | 10 CA / 8 repository-scoped | The Codex CLI machine/setup prerequisite is now live-qualified; the remaining gate is one real authenticated JARVIS workflow run with user-authorized UAC consent and post-setup readiness evidence | The actual target CLI is installed as `codex-cli 0.147.0`; `codex --help` exposes structured non-interactive `exec` and `sandbox` commands. Using the matching standalone binary at `C:\Users\junme\.codex\packages\standalone\releases\0.147.0-x86_64-pc-windows-msvc\bin\codex.exe`, live probes PASS: `sandbox -- cmd /c whoami` returned `legion\codexsandboxoffline`, and `codex sandbox -- cmd /c echo JARVIS_CODEX_CLI_SANDBOX_OK` returned the sentinel. The matching signed helper/resources are present and the Codex-managed sandbox users/group are provisioned. JARVIS’s Windows mediator binds setup requests to provider, distribution, interface, and an exact qualified argument plan; revalidates the helper file hash/path; rejects identity/argument retargeting before launch; and has a production Windows `runas`/`ShellExecuteExW` lifecycle with bounded wait, explicit consent/launch/wait/timeout/helper-failure outcomes, and no success on nonzero exit. `WindowsNativeBroker` exposes only `invoke_provider_setup_repair`; default composition remains fail-closed. `provider_qualification.rs` adds the authenticated setup orchestration: qualified plan → helper identity revalidation → typed broker invocation → independent readiness reprobe. `validateProviderSetupStartRequest` now defines the identity-only authenticated-start request; executable paths and shell arguments cannot cross the protocol boundary. The authenticated native host transport now routes `start_provider_setup` into Core, where provider/distribution/adapter identity is checked and the existing atomic compare-and-set transition enters `SETUP_IN_PROGRESS`; Core never reports readiness from this transition. The authenticated native host also exposes a sanitized provider-status read path for the renderer; it returns only provider identity, distribution, adapter, state, and sanitized failure text. No JARVIS UAC prompt was triggered during tests. Native broker tests PASS 5/5, privilege-mediator tests PASS 7/7, provider-qualification tests PASS 3/3, Core shell tests PASS 7/7, full desktop Rust tests PASS 92/92 with 1 external-account fixture ignored, release `cargo check` PASS, release TypeScript compile PASS, and targeted rustfmt checks PASS. The newly signed desktop build was visually verified: Mission Control rendered normally and displayed `LOCKED · SESSION_LOCKED` with authenticated-transport wording; no blank window or false `NOT_CONNECTED` state remained. | 
| ↳ **7.6** Setup readiness/conformance probe, cancel/failure/repair semantics, version-update invalidation | **IMPLEMENTED** | 7.5 | PLAN §9; RT §15; DS §22; VR §17 | 10 CA / 8 scoped | Live authenticated invocation and complete cancellation/update evidence remain; the bounded probe, post-helper reprobe, atomic provider-version invalidation, and compare-and-set state transitions are implemented | `platform/windows/src/provider_qualification.rs` adds a bounded non-elevated Codex CLI probe that validates absolute installed executable/workdir identity, runs the provider-managed `sandbox` identity and sentinel checks, verifies the structured `exec --help` interface (`--json` and `--sandbox`), caps output, enforces a timeout, rejects nonzero/changed-executable results, and never treats helper exit alone as readiness. `run_authenticated_codex_setup` requires an exact prequalified setup plan, revalidates the helper identity, invokes only the typed native broker operation, and requires a fresh probe afterward. `CoreStateRepository.putProviderProfile` atomically invalidates persisted readiness on qualified profile changes; `transitionProviderSetupState` enforces the existing workflow state machine with expected-version compare-and-set semantics, bounded failure/cancellation outcomes, and no readiness on helper exit. The five non-persistence provider-state tests pass; persistence assertions are currently unverified because the local SQLCipher native module was built for Node ABI 137 while the active Node runtime is ABI 127. Isolated TypeScript compilation of `services/core/src/schema.ts` passes. |
| ↳ **7.7** Authenticated text ConversationService input→context→orchestrator structured-decision pipeline with bounded repair/reformat and no authority bypass | **NOT STARTED** | 7.3, 6.11, 5.6–5.9 | IC §§4,13,17; RT §11; SEC §12; RP §5 | — | — | — |
| ↳ **7.8** GENERALIST/orchestrator plus verifier/synthesis provider profiles and capability/locality routing | **NOT STARTED** | 7.7 | PLAN §9; RP §5; IC §17 | — | — | — |
| ↳ **7.9** WORKSPACE_ENGINEERING profile: exact worktree, controlled env, no unrelated secrets, network denied by default | **NOT STARTED** | 7.3, 6.3, 6.10 | PLAN §9; RT §13; SEC §21 | — | — | — |
| ↳ **7.10** Real Codex sandbox conformance: writes, network, honest read-access claim, ordinary-worker non-elevation | **NOT STARTED** | 7.6, 7.9 | PLAN §9 Mandatory proof; SEC §§21–23; VR §17 | — | — | — |
| ↳ **7.11** Provider process containment, cancellation/timeouts/circuit breaker/crash/restart behavior | **NOT STARTED** | 7.3, 2.4–2.5 | PLAN §9; RT §§14,23; VR §§17,19 | — | — | — |
| ↳ **7.12** Provider fallback/routing that cannot weaken setup/locality/permission/budget/platform support | **NOT STARTED** | 7.8, 7.11 | IC §17; SEC §23; RT §14 | — | — | — |
| ↳ **7.13** Provider quota/usage provenance and support/qualification evidence references | **NOT STARTED** | 7.8, 4.7 | PLAN §9; DS §23; PS §22 | — | — | — |
| ↳ **7.14** Optional provider resume reference plus fresh-session reconstruction from JARVIS-owned state | **NOT STARTED** | 7.11, 4.8 | RT §16; DS §17 | — | — | — |
| ↳ **7.15** Immutable trusted project-policy snapshot injection as scoped context; raw candidates remain untrusted | **NOT STARTED** | 7.9, 6.10–6.11 | PLAN §9; PPT §§7,12 | — | — | — |
| ↳ **7.16** Provider setup/support/health/capability UI and diagnostics distinctions | **NOT STARTED** | 7.4–7.13, 1.13 | OPS §14; UI §§15,25 | — | — | — |
| ↳ **7.17** Windows Codex support tuple and full provider/setup/sandbox/structured-output negative conformance record | **NOT STARTED** | 7.1–7.16 | PLAN §9 Exit; RP §5; VR §17 | — | — | — |
| ↳ **7.CP** SECTION CHECKPOINT — Codex Windows setup/provider/sandbox production support | **NOT STARTED** | 7.1–7.17 | PLAN §9 Exit; PLAN §26 checkpoint; VR §17 | — | — | — |

## Future-section status summary

Section 6 is `VERIFIED` with 6.1–6.15 and 6.CP `VERIFIED`; Section 7 is `IN PROGRESS` with 7.1–7.4 `VERIFIED` and 7.5 `IN PROGRESS` because the Codex CLI prerequisite is live-qualified but JARVIS-owned distribution registration and UAC lifecycle wiring remain, while Sections 8–19 remain `NOT STARTED` except contract-permitted optional rows already marked `DEFERRED` in the reference plan. Their complete subsection definitions, dependencies, traceability, and final release-gate ownership remain in `JARVIS-IMPLEMENTATION-MATRIX-REFERENCE.md` until each parent section becomes active and is promoted into this live board.

## Maintenance invariants

- Routine progress updates modify this live file only.
- Do not use the reference snapshot's historical status fields to roll back live status.
- Structural/dependency/traceability corrections to the reference plan require an explicit matrix-maintenance change and must remain contract-consistent.
- A parent section becomes `VERIFIED` only after all required child rows and its `*.CP` checkpoint pass.
- `Production Complete` remains available only at `19.27` / `19.CP` after exact signed Windows V1 qualification.
- The active release scope is `PRIVATE_INTERNAL`; exact artifact signing remains mandatory, but public-trust and Microsoft Store claims are out of scope.
- The Windows runtime manifest now carries and the native host fail-closed validates the exact private/internal distribution scope, non-public flag, Authenticode trust mode, enrolled certificate thumbprint, current-user target scope, trust-store enrollment, and private timestamp limitation; the rebuilt current local Core bundle passed the full native qualification suite.
- Fresh x64 NSIS rebuild `target/x86_64-pc-windows-msvc/release/bundle/nsis/JARVIS_1.0.6_x64-setup.exe` has SHA-256 `0DB0B5C09170794F06F2BF48829E526CAD4D75B69C5087F9FA0AE269578831D9`, valid Authenticode from the enrolled thumbprint, and a dedicated silent install exited `0`; the installed private/internal runtime passed direct Core qualification, and the installed host signature was valid with a live responsive window before controlled cleanup.
- Core release admission now independently rejects a TUF-authorized manifest whose private/internal distribution scope is altered; focused bootstrap coverage passes under the bundled Node 24 runtime. A fresh local, uncommitted qualification bundle bound to worktree `e3721b9b9d24f3cc41e8f4b7efc0cb9aaee5a73f` and its NSIS installer (`948EB1665EFBDDE3AC770D63405A356CF77286BE561D64206632D4D323858CDF`) passed direct installed qualification and responsive-host checks; this remains qualification evidence, not production-release provenance.
- `tools/release/verify-tuf-metadata-profile.mjs` and its focused unit coverage now verify the production TUF profile shape, separated thresholds, delegated `modules/*` role, signatures, expiry, metadata references, and Windows `FULL_HOST` target identity; it intentionally returns `keyCustodyEvidence=EXTERNAL_REQUIRED`, so local metadata cannot be mistaken for production private-key custody evidence.
- `tools/release/qualify-core-runtime.mjs --production-tuf-profile` now makes the stricter TUF-profile verifier an explicit release-qualification gate and reports `keyCustodyEvidence=EXTERNAL_REQUIRED`; ordinary local qualification remains available without that production-profile flag.
- Re-run after the qualification-gate change: the CT-generated TUF 1.0.35 metadata passed the independent structural verifier with root/targets 2-of-3 signatures, snapshot/timestamp 1-of-1 signatures, and a 2-of-3 `modules/*` delegation; focused verifier tests PASS (`2/2`). Bundled Node `v24.19.0` ran `qualify-core-runtime.mjs --production-tuf-profile` against the current Core runtime and passed authenticated startup/controlled stop, release sequence `1`, source `e3721b9b9d24f3cc41e8f4b7efc0cb9aaee5a73f`, and TUF profile shape `PASS`; the verifier correctly remains `keyCustodyEvidence=EXTERNAL_REQUIRED` because repository-side evidence cannot prove physical custody of the encrypted removable media or a production-signed disaster drill. The three root private-key files were removed from the CT only after matching copies were verified in both encrypted containers; the CT public root JSON files remain.
- The exact packaged Core candidate at source `e3721b9b9d24f3cc41e8f4b7efc0cb9aaee5a73f` passed `tools/release/qualify-backup-restore.mjs` with TUF target `runtime-manifest.json` SHA-256 `d323b6acaaaa0d508365a0a458485e8cb99b8eb01624c0bb90cc087e2be82609`; the bounded drill passed wrong-factor rejection, clean-profile restore, fresh DB_DEK protection commit, recovery-marker write, and `REAUTH_REQUIRED` credential reconciliation without exporting the recovery secret. This is packaged-candidate qualification evidence, not proof of independently preserved signing-key custody or a production-signed release drill.
- The 3.15 restore path now converts a post-publication fresh-DB_DEK commit failure into typed `BACKUP_PACKAGE_DB_DEK_PROTECTION_FAILED` evidence while preserving the restored database and recovery marker; focused backup-package coverage remains PASS (`6/6`) and Core typecheck PASS.
- After the two encrypted removable-media containers were physically ejected and stored offline, the exact signed packaged candidate was re-qualified with a fresh external 32-byte recovery secret: `tools/release/qualify-backup-restore.mjs` PASS, authenticated Core transport PASS, controlled stop PASS, wrong-factor rejection PASS, clean-profile restore PASS, fresh DB_DEK commit PASS, recovery marker PASS, and `REAUTH_REQUIRED` reconciliation PASS; the secret file was removed after the drill. The verifier remains `keyCustodyEvidence=EXTERNAL_REQUIRED`, so this does not yet constitute final production-signed disaster-drill evidence.
- The same TUF-bound packaged candidate was independently re-qualified with `pnpm release:qualify-core-runtime -- --production-tuf-profile`: bundled Node `v24.18.0`, TUF `1.0.35`, source `e3721b9b9d24f3cc41e8f4b7efc0cb9aaee5a73f`, authenticated transport PASS, protocol major `1`, controlled stop PASS, and empty stdout/stderr; `keyCustodyEvidence` remains truthfully `EXTERNAL_REQUIRED`.
- A fresh NSIS rebuild from the corrected TUF resource was Authenticode-valid under the enrolled private/internal certificate (SHA-256 `2AEB7339C4745257F64D3BA4BC87369BFF4D6D233FB3EA87D8C930C3B1CB3860`), installed into an isolated directory with exit code `0`, and verified there with TUF profile shape PASS, `modules/*` delegation, authenticated Core transport PASS, controlled stop PASS, and the complete clean-profile restore drill PASS. This supersedes the earlier stale-installed-candidate limitation; 3.15 and 3.16 are now verified with the external custody evidence recorded above.
- Cross-language `JARVIS_BACKUP_V1` golden vectors were added as a manifest-bound synthetic fixture and independently checked by TypeScript and Rust; the focused vector suite, normal unit layer, full Windows Rust suite, typecheck, dependency/provenance, and secret-scan checks passed. The dedicated Section 3 checkpoint checker and its negative tests then passed against the exact packaged restore evidence, so Section 3 is VERIFIED. This does not declare Production Complete.
