# Section 1.1 Tauri + React Desktop Foundation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: use test-driven-development before implementation and verification-before-completion before any completion claim.

**Goal:** Implement subsection 1.1 as a real Tauri 2 / Rust + React / TypeScript desktop workspace whose production WebView is served from bundled local assets, while preserving the exact authority boundaries and leaving all 1.2+ responsibilities unclaimed.

**Architecture:** `apps/desktop` becomes the real frontend workspace built by Vite; `apps/desktop/src-tauri` becomes a minimal Rust/Tauri application crate. The Tauri production `frontendDist` points only at the generated local `dist` directory. No Core connection, process execution, credential access, provider access, filesystem authority, or consequential custom command surface is introduced in 1.1.

**Pinned stack:** Node 24.18.0; pnpm 11.21.0; TypeScript 6.0.3; Rust 1.97.1; Tauri 2.11.5; tauri-build 2.6.3; @tauri-apps/api 2.11.1; @tauri-apps/cli 2.11.4; React/react-dom 19.2.8; Vite 8.1.5; @vitejs/plugin-react 6.0.3; @types/react 19.2.17; @types/react-dom 19.2.3.

## Windows V1 qualification correction

This non-normative implementation plan is subordinate to the active v1.0.6 contract suite. For every application/native `host` reference below, the host is the active Release Profile host: `WINDOWS + FULL_HOST + x64`. Native Tauri/Rust qualification SHALL run on Windows with the pinned MSVC target. Linux portability remains an architecture/import/platform-capability property; Linux/Ubuntu Tauri host compilation, WebKitGTK/GTK host prerequisites, and Linux-to-MSVC cross-builds are not Section 1.1 acceptance gates.

---

## Task 1 — Activate Section 1.1 and create fail-closed subsection enforcement

**Files**
- Modify: `docs/implementation/JARVIS-IMPLEMENTATION-MATRIX.md`
- Create: `tools/ci/check-desktop-foundation.mjs`
- Create: `tests/layers/unit/desktop-foundation.test.mjs`
- Modify: `package.json`
- Modify: `.github/workflows/static-ci.yml`
- Create: `.gitattributes` when required to make the clean Windows checkout deterministic without rewriting tracked source

**Steps**
1. Promote the complete Section 1 rows from the reference matrix into the live matrix and mark only `1.1` / Section 1 `IN PROGRESS`. Keep 1.2–1.CP `NOT STARTED`.
2. Write tests first for a `checkDesktopFoundation` validator. The tests must initially fail on the current declaration-only desktop stub.
3. Required failing mutations:
   - missing `apps/desktop/src-tauri/Cargo.toml`;
   - Tauri runtime pin differs from toolchain baseline;
   - tauri-build pin differs from baseline;
   - `@tauri-apps/api` differs from baseline;
   - production `frontendDist` is a URL or anything other than `../dist`;
   - Tauri crate missing from root Cargo workspace;
   - `build:web` is not a real Vite production build;
   - required CI `Desktop foundation contract` gate removed;
   - required explicit Windows Tauri compile gate removed;
   - mandatory Section 1.1 `static-ci` runner drifts away from native `windows-2025`;
   - Linux/Ubuntu Tauri host prerequisites are reintroduced as acceptance gates;
   - tracked Rust source is rewritten in CI to normalize line endings instead of validating a clean checkout;
   - repository-owned LF/binary checkout policy required by native Windows CI is missing;
   - Tauri context-generation pairing is changed back to a build-context include without configured build-time codegen.
4. Implement the checker only after observing the red test state.
5. Add root script `desktop:foundation:check` and CI steps:
   - `Desktop foundation contract` → `pnpm desktop:foundation:check`
   - `Desktop Tauri Windows build` → `cargo check --locked -p jarvis-desktop --target x86_64-pc-windows-msvc`
6. Preserve the generic Cargo/Rust gates, but execute the application/native host qualification on native Windows. Future Linux portability remains enforced by architecture/import/platform-capability tests rather than Linux Tauri host compilation.

**Acceptance**
- Tests prove the current pre-1.1 workspace and every listed weakening mutation fail closed.
- Checker is deterministic/read-only and reports stable violation codes.
- Mandatory 1.1 CI/native qualification is Windows-hosted; no Linux Tauri host prerequisite is required.
- Matrix truthfully shows active implementation, not VERIFIED.

## Task 2 — Build the real React/Vite renderer workspace

**Files**
- Modify: `apps/desktop/package.json`
- Create: `apps/desktop/index.html`
- Create: `apps/desktop/vite.config.ts`
- Modify: `apps/desktop/tsconfig.json`
- Modify: `apps/desktop/tsconfig.build.json`
- Create: `apps/desktop/src/main.tsx`
- Create: `apps/desktop/src/App.tsx`
- Create: `apps/desktop/src/styles.css`
- Remove or narrow: `apps/desktop/src/index.ts` if it conflicts with the real application entry point
- Modify: root `tsconfig.json` only as required to typecheck TSX/DOM safely

**Steps**
1. Pin the approved stack exactly in the desktop manifest; no ranges.
2. Set `build:web` to run strict TypeScript checking followed by `vite build`.
3. Configure Vite with React plugin, fixed local dev port `5173`, `strictPort: true`, and deterministic production `dist` output.
4. Render a minimal inert JARVIS desktop foundation screen. It may state that the desktop foundation is active; it must not imply Core/providers/permissions/systems are operational.
5. Use no CDN, external executable script, remote stylesheet, telemetry, or remote application origin.
6. Keep styling minimal and aligned with current dark-theme identity without implementing the later design-system/brand/font subsection.

**Acceptance**
- `pnpm build:ui` produces `apps/desktop/dist/index.html` plus local assets.
- Root strict TypeScript checks remain green.
- No consequential renderer authority is added.

## Task 3 — Add the real Tauri 2 Rust host

**Files**
- Modify: root `Cargo.toml`
- Create: `apps/desktop/src-tauri/Cargo.toml`
- Create: `apps/desktop/src-tauri/build.rs`
- Create: `apps/desktop/src-tauri/tauri.conf.json`
- Create: `apps/desktop/src-tauri/src/lib.rs` only if the selected Tauri structure requires it
- Create: `apps/desktop/src-tauri/src/main.rs`
- Remove/replace: `apps/desktop/src-tauri/README.md`

**Steps**
1. Add `apps/desktop/src-tauri` to the root Cargo workspace.
2. Pin `tauri = "=2.11.5"` and `tauri-build = "=2.6.3"` exactly.
3. Implement the smallest safe Tauri application entry point with no consequential custom commands/plugins. Use the exact supported context-generation pairing for the pinned versions; with the standard `tauri_build::build()` hook and no build-time codegen configuration, use `tauri::generate_context!()` rather than consuming a non-generated `tauri-build-context.rs`.
4. Configure build hooks for the desktop package and production `frontendDist: "../dist"`; development may use `http://localhost:5173` only as a dev URL.
5. Do not claim 1.2 CSP/capability/navigation/devtools hardening; do not add remote-origin capability.
6. Do not start Core or platform backends.
7. Keep the non-Windows entry path an explicit unsupported stub; it is an architecture guard, not Linux runtime qualification.

**Acceptance**
- Tauri host compiles under pinned Rust on the native Windows V1 runner.
- `cargo check --locked --workspace --target x86_64-pc-windows-msvc` passes on native Windows.
- `cargo check --locked -p jarvis-desktop --target x86_64-pc-windows-msvc` passes on native Windows.
- Production configuration is bundled-local and checker-enforced.
- No Linux Tauri host build is used as substitute evidence.

## Task 4 — Regenerate locks and complete dependency provenance

**Files**
- Modify: `pnpm-lock.yaml`
- Modify: `Cargo.lock`
- Modify: `third_party/provenance.json`
- Modify: `THIRD_PARTY_NOTICES.md`
- Modify: `tools/toolchain/toolchain-baseline.json`
- Modify: `tools/toolchain/toolchain-baseline.schema.json`
- Modify: `tools/toolchain/verify-toolchain.mjs`
- Modify/add tests for toolchain qualification as required

**Steps**
1. Regenerate `pnpm-lock.yaml` from the exact manifests; do not hand-author transitive resolution data.
2. Regenerate `Cargo.lock` from the exact Cargo manifests.
3. Inventory every external package represented in both lockfiles because `check-dependencies.mjs` requires every resolved npm/Cargo package to have approved provenance.
4. Record exact source/version/license/review status for each new dependency and transitive dependency; update notices accordingly.
5. Change Tauri qualification metadata from `DECLARED_RELEASE_FACT_NOT_YET_IMPLEMENTED` to a truthful subsection-level state such as `DESKTOP_FOUNDATION_IMPLEMENTED_NOT_RELEASE_QUALIFIED`; update schema/verifier tests so this is explicit and fail-closed.
6. Do not label Tauri as fully release-qualified or 1.2-security-qualified.

**Acceptance**
- frozen pnpm install succeeds with ignored lifecycle scripts;
- Cargo `--locked` checks succeed;
- dependency inventory and provenance/license checks pass with no omitted lockfile package.

## Task 5 — Verify, evidence, score, and prepare exact candidate review

**Files**
- Create: `docs/implementation/evidence/1.1-tauri-react-desktop-foundation.md`
- Modify: `docs/implementation/JARVIS-IMPLEMENTATION-MATRIX.md`

**Verification commands / gates**

All application/native Rust/Tauri gates below are authoritative only when executed on the native Windows V1 runner. Platform-neutral checks remain mandatory but do not create Linux runtime qualification.

- exact toolchain verification
- `pnpm format:check`
- `pnpm schema:check`
- contract generated/manifest/drift checks
- governance check
- secret scan
- dependency inventory
- provenance/license check
- root strict TypeScript check
- root TypeScript build
- `pnpm build:core`
- `pnpm build:ui`
- `pnpm desktop:foundation:check`
- architecture enforcement
- deterministic tests
- `pnpm audit --audit-level high`
- `cargo fmt --all -- --check`
- `cargo clippy --locked --workspace --all-targets --all-features -- -D warnings`
- `cargo check --locked --workspace --all-targets --all-features`
- `cargo check --locked --workspace --target x86_64-pc-windows-msvc`
- explicit `cargo check --locked -p jarvis-desktop --target x86_64-pc-windows-msvc`
- Phase 0 checkpoint remains green

**Steps**
1. Run every platform-neutral check available in the working environment and require the complete native Windows GitHub qualification chain on the exact candidate SHA. Fix failures; do not soften gates or add Linux runtime work to make Windows qualification pass.
2. Write evidence containing exact source commit/tree, tests, commands, results, contract traceability, non-goals, failure/recovery semantics, atomicity/idempotency analysis, and scores.
3. Score harshly. `Contract Accuracy` must equal `10/10`; applicable repository thresholds must meet or exceed 8/10. Otherwise leave `1.1` non-VERIFIED and list the exact gap.
4. Re-fetch live `master` before every repository commit/write that follows; reconcile any movement rather than overwriting it.
5. Push/retain the exact candidate branch and require GitHub `static-ci` success on that exact SHA, with native Windows Tauri qualification passed.
6. Request independent review of the exact candidate. Do not integrate while review has an unresolved blocking finding.
7. Only after review passes: re-fetch live `master`, verify expected ancestry/diff, integrate non-force, and verify authoritative post-integration `static-ci` on the resulting master SHA.
8. Only then record `1.1 = VERIFIED`, advance pointer to `1.2`, and preserve the exact integration/post-CI evidence through a separate controlled update if needed.

## Scope stop

Stop after 1.1. Do not begin 1.2 automatically. A green build is not authority to expand scope.
