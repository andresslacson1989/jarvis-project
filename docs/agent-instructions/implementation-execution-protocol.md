# JARVIS Implementation Execution Protocol Pointer

This file is a non-normative navigation aid. It does not duplicate, replace, narrow, or supersede repository governance.

The current implementation execution protocol is the complete **Contract implementation execution protocol** section in the repository-root `AGENTS.md`. Contributors SHALL read that section directly after revalidating live `master`, the active v1.0.7 manifest, the Implementation Plan, and the live implementation matrix.

If this pointer and `AGENTS.md` ever disagree, `AGENTS.md` controls and dependent work stops until this pointer is corrected.
