{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-local-ui",
  "description": "The bundled JARVIS renderer has no privileged native capability permissions.",
  "windows": ["main"],
  "permissions": ["opener:allow-open-url"]
}
