# Managed Module Boundary

`modules` is the namespace for JARVIS-managed modules under the contract-defined module registry and supply-chain trust model.

Directory presence, publisher identity, or a module's own signature never grants execution authority or `SUPPORTED` status. Module execution class, compatibility, trusted catalog authorization, containment, activation, rollback, and qualification are implemented in their later owning sections.
