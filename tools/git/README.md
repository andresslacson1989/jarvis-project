# Git Tool Boundary

`tools/git` is reserved for typed local Git capabilities behind the common JARVIS execution and authorization boundaries.

Git operations must use canonical project/workspace identity and cannot bypass permission, authority, expected-state, audit, or platform-process constraints. Implementation belongs to later tool and Git/GitHub sections.
